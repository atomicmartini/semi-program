# 반도체 뉴스 데일리 — PRD

> 작성 2026-08-22 · 초안
> 규칙은 `AGENTS.md`, 수집처는 `data/sources.md`.
> **[기획자]** 표시는 사용자가 직접 정한 것, **[제안]** 은 이 문서에서 채운 것으로 확인이 필요하다.

---

## 1. 문제

**[기획자]**

반도체 업계에서는 매일 일이 벌어지는데 소식이 여러 사이트에 흩어져 있어 따라가기 어렵다.
그리고 **예전에 일어난 일과 오늘 일을 이어서 볼 수 있는 곳이 없다.**

두 번째가 이 제품의 핵심이다. 첫 번째만 푸는 것은 RSS 리더와 다르지 않다.

### 해결하려는 실패

- 오늘 기사만 읽고 "왜 이게 뉴스인지" 모른 채 넘어간다
- 3주 전 소문이 오늘 공식 발표로 확정된 것을 알아채지 못한다
- 기업들이 경쟁하는지 협력하는지 파편적인 기사로는 안 보인다

---

## 2. 사용자

**[기획자]**

| 누가 | 무엇이 필요한가 |
| --- | --- |
| 반도체 업계 취준생 | 업계 흐름과 기업 관계를 말할 수 있을 정도로 이해 |
| 트렌드를 매일 받고 싶은 사람 | 하루 한 번, 짧게, 놓친 것 없이 |
| 관련 개념을 공부하고 싶은 사람 | 모르는 용어가 나왔을 때 막히지 않기 |

**언제** — 반도체가 궁금할 때. 하루 한 번, 전날 있던 일을 확인.
**어떻게** — 웹사이트.

> **[제안 · 확인 필요]** 세 번째 사용자("개념을 공부하고 싶은 사람")를 넣으면
> **용어 설명 기능**이 필요해진다. 취준생은 `HBM` `첨단 패키징` `CoWoS` 를 모를 수 있다.
> 이걸 범위에 넣을지 정해야 한다 (§7 열린 결정 참조).

---

## 3. 범위

### 하는 것

| # | 기능 | 우선순위 |
| --- | --- | --- |
| F1 | 매일 `data/sources.md` 의 사이트에서 기사를 모은다 | `P0` |
| F2 | 반도체 무관 기사를 키워드로 거른다 (코드) | `P0` |
| F3 | 같은 사건을 다룬 기사를 하나로 묶는다 (코드) | `P0` |
| F4 | 점수 순으로 **하루 10건**을 고른다 | `P0` |
| F5 | 6개 카테고리로 분류한다 | `P0` |
| F6 | 기사에서 기업·관계를 뽑는다 | `P0` |
| F7 | **과거 사건과 잇고, 무엇이 달라졌는지 보여준다** | `P0` |
| F8 | 매일 웹페이지로 내보낸다 | `P0` |
| F9 | 카테고리·기업으로 걸러 보기 | `P1` |
| F10 | 용어 설명 | `P2` — 확인 필요 |

**카테고리 6개 + 미분류** (`AGENTS.md` R5)
`파운드리·공정` `메모리` `AI·가속기` `장비·소재` `정책·규제` `투자·실적` `미분류`

### 안 하는 것 — **[제안]**

명시해 두지 않으면 계속 늘어난다.

- 실시간 알림 (하루 한 번으로 충분하다)
- 로그인·개인화·즐겨찾기
- 댓글·커뮤니티
- 영문 기사 전문 번역 (요약만 한국어로)
- **투자 추천·주가 전망** — §6 레드라인 참조
- 원문 기사 본문 전체 저장·게시

---

## 4. 어떻게 보여줄 것인가 — **[제안]**

기획자가 "아직 잘 모르겠다"고 한 부분이다. 화면 두 개로 제안한다.

### 4-1. 오늘 페이지 (기본 화면)

각 뉴스 아래에 **과거 흐름이 접혀 있다.** 이게 이 제품의 정체성이다.

```
2026-08-22  ·  10건  ·  수집 5곳 모두 정상

┌─ [메모리]                                        보도됨
│  SK하이닉스, HBM4 양산 준비 착수
│  (요약 두 줄)
│
│  ↳ 이어지는 흐름 3건                          [펼치기]
│      08-05  SK하이닉스 HBM4 샘플 공급          보도됨
│      07-22  엔비디아 HBM4 공급사 선정 착수      보도됨
│
│  달라진 점: 07-22 에는 '검토' 였고 오늘은 '양산 준비' 다
│  원문 → thelec.kr  ·  발행 08-22 09:14  ·  수집 08-22 18:00
└─
```

- 카테고리 태그와 **상태 배지**(`확정` / `보도됨` / `확인 필요`)를 항상 함께
- **"달라진 점"** 한 줄이 핵심 — 과거와 오늘의 차이. 없으면 안 쓴다
- 이어지는 흐름이 없으면 그 줄을 아예 안 보여준다 (없는 걸 지어내지 않는다)

### 4-2. 흐름 페이지 (사건 하나를 시간순으로)

'이어지는 흐름'을 누르면 그 사건만 시간축으로 본다.

```
HBM4 공급망  ·  기사 7건  ·  2026-07-22 ~ 2026-08-22

07-22  엔비디아, HBM4 공급사 선정 착수        보도됨   EE Times
08-05  SK하이닉스 HBM4 샘플 공급               보도됨   더일렉
08-22  SK하이닉스 양산 준비 착수               보도됨   더일렉
       └ 관련 기업: SK하이닉스 · 엔비디아 · 삼성전자
```

### 안 만드는 것 — 관계도(그래프)

기업을 점으로, 관계를 선으로 그리는 그림은 **보기엔 좋지만 지금 만들면 안 된다.**
데이터가 며칠치뿐일 때는 선이 몇 개 없어 빈약하고, 만드는 비용은 크다.
**3개월쯤 쌓인 뒤 다시 판단한다.**

### 첫 며칠 문제 — **[제안]**

데이터가 없으면 F7(과거 연결)이 아무것도 못 한다.
**처음 2~3주는 그냥 뉴스 목록**이고, 그 사실을 화면에 적는다.
"아직 흐름을 잇기에 데이터가 부족합니다 (수집 3일차)" 처럼.

---

## 5. 배포 — **[제안]**

기획자 질문: "github 에 올려서 매일 git update 하는 식으로 하면 되려나?"

**맞다. 그리고 그게 이 제품에 가장 좋은 방식이다.**

```
매일 정해진 시각
  GitHub Actions 가 자동 실행
    → 기사 수집 (RSS)
    → 거르기 · 묶기 · 10건 선별      (코드)
    → 분류 · 관계 추출               (모델)
    → 과거와 잇기                    (코드)
    → docs/2026-08-22.html 생성
    → 저장소에 커밋
  GitHub Pages 가 그 파일을 그대로 서비스
```

**왜 좋은가**

| | |
| --- | --- |
| 서버가 필요 없다 | 정적 파일만 올리면 된다. `server.py` 불필요 |
| 공짜다 | Actions 무료 한도 안에서 충분하다 |
| 기록이 곧 백업이다 | 매일 커밋이 쌓여 어제 화면을 그대로 되돌려 볼 수 있다 |
| 실패가 눈에 보인다 | Actions 가 빨간불로 알려준다 |

**주의할 점**

- 열쇠는 GitHub Secrets 에 넣는다. 저장소에 절대 올리지 않는다
- 저장소가 공개면 **수집한 기사 요약도 공개**된다. 뉴스 요약이라 문제는 없지만 알고 있어야 한다
- Actions 는 정시에 안 돌 수 있다 (몇 분~수십 분 지연). 하루 한 번이라 문제 없다

> **이 결정이 §3 F8 과 아키텍처를 바꾼다.** `my-app` 에서 만든 `server.py` 방식이 아니라
> **정적 사이트 생성** 방식이 된다. FastAPI 는 안 쓴다.

---

## 6. 어기면 안 되는 것 — **[제안]**

기획자가 "잘 모르겠다"고 한 부분. 대부분 `AGENTS.md` 에 이미 규칙으로 들어 있다.

### 절대 하면 안 되는 것

| # | 레드라인 | 왜 |
| --- | --- | --- |
| N1 | **기사에 없는 관계를 지어내지 않는다** | 이걸 어기면 제품 전체가 무의미하다. 가장 큰 실패 |
| N2 | **`[2차]` 만으로 `확정` 이라 하지 않는다** | 소문을 사실로 만들면 신뢰가 끝난다 |
| N3 | **원문 전체를 복제하지 않는다.** 요약 + 링크만 | 저작권 |
| N4 | **투자 판단으로 쓰라고 말하지 않는다** | 아래 참조 |
| N5 | **API 열쇠를 저장소에 올리지 않는다** | |
| N6 | **RSS 가 주는 것만 쓴다. 사이트를 긁지 않는다** | 이용약관·robots.txt |
| N7 | **수집 실패를 조용히 넘기지 않는다** | '뉴스 없음' 과 '수집 실패' 는 다르다 |
| N8 | **자른 기사 수를 숨기지 않는다** | 10건 상한을 넘겨 자를 때 몇 건인지 밝힌다 |

### N4 를 따로 적는 이유

기업 간 **경쟁·협력 구도**는 그 성격상 투자 판단처럼 읽힌다.
사용자에 취준생이 있고, 반도체는 주식 관심이 높은 업종이다.

- 화면 하단에 **"투자 조언이 아닙니다"** 를 항상 표시한다
- 주가·목표가·매수매도 의견을 다루지 않는다
- `투자·실적` 카테고리는 **사실(설비투자 발표, 분기실적)만** 담는다. 전망은 담지 않는다

---

## 7. 성공 판정 — **[제안]**

기획자 안: "매일 10개의 뉴스가 잘 열리는지"
→ 이건 **살아 있는지 확인하는 것**이지 성공 판정이 아니다. 세 층으로 나눈다.

### 7-1. 살아 있는가 (자동 확인)

| 지표 | 목표 |
| --- | --- |
| 7일 연속 실행 실패 | 0회 |
| 수집처 응답 실패 시 리포트 표시 | 100% |
| 하루 산출 기사 수 | 10건 (부족하면 부족하다고 표시) |

### 7-2. 맞는가 (품질 — 사람이 표본 확인)

여기가 진짜 중요하다. **`cases.json` 으로 검산**한다 (`my-app` 방식 그대로).

| 지표 | 목표 | 재는 법 |
| --- | --- | --- |
| 관계 인용구가 원문에 실제로 있는 비율 | **100%** | 코드가 자동 검산 (`AGENTS.md` R3) |
| 카테고리 오분류율 | 10% 이하 | 표본 20건 사람이 확인 |
| 잘못 이은 비율 (다른 사건을 이었다) | **5% 이하** | 표본 20건. 잘못 잇는 쪽이 못 잇는 쪽보다 나쁘다 |
| 기업명 정규화 실패 | 미확인 목록에 쌓이고 있는가 | `data/unknown_companies.md` 확인 |

### 7-3. 쓸모가 있는가 (제품 성공)

**이게 최종 판정이다.**

> **"이 연결은 몰랐다" 싶은 항목이 주 1회 이상 나오는가.**

이게 안 나오면 잘 만든 RSS 리더일 뿐이다.
한 달 써 보고 이 질문에 "아니오" 면 F7(과거 연결) 설계를 다시 봐야 한다.

보조 지표
- 만든 사람이 실제로 매일 열어 보는가 (2주 연속)
- 취준생 1~2명에게 보여줬을 때 "이거 쓸 것 같다" 는 반응이 나오는가

---

## 8. 열린 결정 — 정해야 진행 가능

| # | 정할 것 | 왜 지금 |
| --- | --- | --- |
| Q1 | **`[공식]` 출처를 넣을 것인가** | 안 넣으면 모든 관계가 영원히 `보도됨` 이다 (N2·R1 이 반쪽이 된다). 기업 뉴스룸 RSS 확인 필요 |
| Q2 | **용어 설명(F10)을 범위에 넣을 것인가** | 사용자에 "공부하고 싶은 사람" 이 있는데 기능이 없다 |
| Q3 | 영문 기사 요약을 한국어로 낼 것인가 | 수집처 5곳 중 4곳이 영어다. 안 하면 취준생이 못 읽는다 |
| Q4 | 하루 10건을 카테고리별로 배분할 것인가 | 안 하면 메모리 뉴스만 10건 나오는 날이 생긴다 |
| Q5 | 공개 저장소로 할 것인가 | 공개면 GitHub Pages 가 무료다. 비공개면 유료 요금제가 필요할 수 있다 |

---

## 9. 알려진 한계

- **처음 2~3주는 과거 연결이 거의 안 나온다.** 데이터가 쌓여야 한다
- 수집처 5곳 중 4곳이 영어라 국내 소식 비중이 낮다
- 무료 모델의 호출 한도에 걸리면 그날 처리가 중단될 수 있다 (10건 상한으로 완화)
- RSS 요약(250자 내외)만 읽으므로 기사 뒷부분에만 나온 관계는 놓친다
- `[2차]` 출처끼리 서로 인용한 경우 원점을 잘못 짚을 수 있다
