# PLAN — 2주를 자른 결과

> 왜·누구·무엇은 `PRD.md`, 규칙은 `CLAUDE.md`, 수집처는 `data/sources.md`.
> 이 문서는 며칠 단위로 바뀐다.

## 2주 리듬

| 일자 | 산출물 | 핵심 |
| --- | --- | --- |
| **Day 1** | `PRD.md` · `CLAUDE.md` · `data/sources.md` | ✅ 완료 (2026-08-22). 수집처 8곳 실제 검증 |
| **Day 2** | `fetch.py` · **배포된 URL** | 빈 페이지라도 GitHub Pages 에 올린다. 여기서 미루면 절반이 무너진다 |
| **Day 3—4** | 슬라이스 1 — 거르기 · 묶기 · 10건 선별 | 모델 안 씀. 전부 코드 |
| **Day 5—6** | 슬라이스 2 — 분류 · 한국어 요약 · 관계 추출 | 모델 씀. 매일 배포 |
| **Day 7** | **취준생 1명에게 보여주기** | 중간 체크포인트. 이때 화면에 뉴스 10건이 보여야 한다 |
| **Day 8—10** | 슬라이스 3 — 과거 사건과 잇기 | 피드백 반영. **새 기능 추가 없음** |
| **Day 11—12** | 버그 수정 · 화면 정리 | 기능 동결 |
| **Day 13—14** | 데모 리허설 | 3분 안에 끝나야 한다 |

## 수직 슬라이스

각 슬라이스는 **끝까지 도는 한 흐름**이다. 단계마다 새로 도입하는 것은 하나뿐.

| # | 파일 | 새로 도입 | 완료 조건 (실행 명령) |
| --- | --- | --- | --- |
| S1 | `fetch.py` | 수집 | `python fetch.py` → `data/articles/<오늘>.json` 생성, 건수 출력. 다시 실행하면 새 기사 0건 |
| S2 | `filter.py` | 키워드 거르기 | `python filter.py <날짜>` → 반도체 관련만 남고 몇 건 걸렀는지 출력 |
| S3 | `cluster.py` | 중복 묶기 | `python cluster.py <날짜>` → 같은 사건이 한 묶음으로 나옴 |
| S4 | `select.py` | 10건 선별 | `python select.py <날짜>` → 카테고리당 최대 3건, 총 10건. 자른 건수 출력 |
| S5 | `extract.py` | 분류 · 요약 · 관계 (모델) | `python extract.py <기사>` → 카테고리·한국어 요약·관계가 정해진 형식으로 나옴 |
| S6 | `link.py` | 과거와 잇기 | `python link.py <날짜>` → 오늘 사건이 과거 어느 사건의 후속인지, 점수와 함께 |
| S7 | `render.py` | 페이지 생성 | `python render.py <날짜>` → `docs/<날짜>.html` 생성 |
| S8 | `.github/workflows/` | 매일 자동 실행 | Actions 가 돌고 커밋이 쌓임 |

**S1—S4 는 모델을 안 쓴다.** 수집과 정리가 안 깨지는지 먼저 확인한다.

각 파일은 **이름 있는 함수 + 직접 실행** 둘 다 된다.
`fetch_articles(날짜)` `filter_articles(기사들)` `cluster_articles(기사들)` `select_top(묶음들)` `extract_relations(기사)` `link_to_past(묶음들)` `render_page(날짜)`

## 명세 — 코드 상수가 될 것들

> `CLAUDE.md` 에서 내려온 것. **코드에 상수로 박고 여기는 참고용으로만 둔다.**
> 두 곳에 적으면 반드시 어긋난다. 늘릴 때는 먼저 질문한다.

**카테고리 6 + 미분류** — 매일 같아야 비교가 된다. 애매하면 억지로 넣지 않고 `미분류`.
`파운드리·공정` `메모리` `AI·가속기` `장비·소재` `정책·규제` `투자·실적` `미분류`

**관계 종류 10** — 열린 집합으로 두면 집계가 안 된다.
`공급` `투자` `인수` `제휴` `소송` `증설` `감산` `수주` `규제` `인력이동`

**관계 상태 3** — `확정`(`[공식]` 출처 있음) / `보도됨`(`[2차]`만) / `확인 필요`

**10건 선별 점수** — `[공식]` +3 · 묶음 1건당 +1(최대 +3) · 관심 기업 +1 · 기존 사건 후속 +2
동점이면 최신순. **카테고리당 최대 3건**, 10건 안 차면 점수 순으로 채운다.

**기사 연결 판정** — 공통 기업 수 · 시간 거리 · 키워드 겹침으로 점수.
임계값 아래는 잇지 않고 `확인 필요`. **왜 이었는지 점수와 함께 남긴다.**

**고유 열쇠** — 원문 링크. 같은 기사를 두 번 처리하지 않는다.

**표기** — 모든 사실에 발행시각·수집시각·원문 링크. 모르면 `—`.

## 화면 설계

### 오늘 페이지 (기본)

```
2026-08-22  ·  10건  ·  수집 8곳 중 8곳 정상

┌─ [메모리]                                        보도됨
│  SK하이닉스, HBM4 양산 준비 착수
│  (한국어 요약 두 줄)
│
│  ↳ 이어지는 흐름 3건                          [펼치기]
│      08-05  SK하이닉스 HBM4 샘플 공급
│      07-22  엔비디아 HBM4 공급사 선정 착수
│  달라진 점: 07-22 엔 '검토'였고 오늘은 '양산 준비'다
│
│  원문 → thelec.kr  ·  발행 08-22 09:14  ·  수집 08-22 18:00
└─
```

- 카테고리 태그 + 상태 배지(`확정`/`보도됨`/`확인 필요`)를 항상 함께
- **"달라진 점" 한 줄이 이 제품의 정체성.** 없으면 그 줄을 안 쓴다
- 이어지는 흐름이 없으면 그 영역을 아예 안 보여준다
- 하단에 **"투자 조언이 아닙니다"** 항상 표시

### 흐름 페이지 (사건 하나를 시간순으로)

'이어지는 흐름'을 누르면 그 사건만 시간축으로 본다.

### 첫 며칠 문제

데이터가 없으면 S6 이 아무것도 못 한다. 두 가지로 푼다.

1. 첫 실행 때 **각 피드가 주는 과거분을 한 번에 받아 쌓는다** (Semiconductor Digest 는 100건을 준다)
2. 그래도 부족하면 화면에 적는다 — "수집 3일차, 흐름을 잇기엔 데이터 부족"

## 아직 안 정한 것

| # | 정할 것 | 언제까지 |
| --- | --- | --- |
| Q6 | 카테고리당 3건이 맞는가 | Day 5, 며칠 돌려 보고 |
| Q7 | 매일 몇 시에 돌릴 것인가 | Day 2, Actions 만들 때 |
| Q8 | 용어 사전 초기 항목 몇 개로 시작 | Day 8, 0개로 시작하면 용어 설명이 아무것도 안 한다 |
| Q9 | **Day 7 에 보여줄 취준생 1명이 누구인가** | Day 3 까지 |

## 다음 할 일

`evals/cases.md` — 입력·기대·판정 10줄. **실패해야 정상인 케이스 3개 포함.**
이 10줄이 그대로 작업 지시의 "완료 조건"이 된다.
