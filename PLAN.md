# PLAN — 이번 세션에 자를 슬라이스

> 왜·누구·안 만들 것은 `PRD.md`, 규칙은 `CLAUDE.md`, 전체 작업 쪼갬은 `WBS.md`.
> 지난 판은 `PLAN_pre.md` 에 보관돼 있다 — **아카이브이므로 참조하지 않는다.**

## 이번 세션의 성공 기준

> **배포된 URL 에서 핵심 기능 하나가 처음부터 끝까지 동작한다.**

지금 `https://semi-program.vercel.app` 에는 손으로 만든 **예시 화면**만 있다.
`fetch.py` 와 `filter.py` 는 도는데 그 결과가 화면까지 이어지지 않는다.
이 세션은 그 사이를 잇는다.

## 정한 것

| 물음 | 답 | 왜 |
| --- | --- | --- |
| 이번의 '하나' | **뉴스가 화면에 뜬다** | 수집→거르기→선별→정적 HTML→배포. 모델이 필요 없어 확실히 끝낼 수 있다 |
| 하루 10건이 안 나옴 | **10건은 상한.** 있는 만큼만 | 67일 중 10건 이상인 날이 2일뿐이다. 지어내지 않는다는 규칙과도 맞다 |
| 모델을 어디서 | **로컬에서 돌리고 결과를 커밋** | 이번엔 모델 자체를 안 쓴다. 열쇠·비용·환각이 이번 걱정거리가 아니게 된다 |
| '달라진 점' 문장 | **안 쓴다** | 두 기사를 비교해 새 문장을 만드는 일이라 "기사에 없는 것을 지어내지 않는다" 와 부딪힌다 |

---

## 만들 것

### 1. `select.py` — 무엇을 보여줄지 고른다

```
python select.py <날짜>
```

- `filter.py` 의 `filter_articles()` 결과를 받는다. 거르기를 다시 만들지 않는다
- **카테고리당 최대 3건, 전체 최대 10건.** 모자라면 있는 만큼만
- 출력: 고른 건수 · 카테고리별 건수 · **자른 건수**
- `select_top(articles)` 로 노출, 명령행 처리는 `__main__` 에만

### 2. `render.py` — 정적 HTML 을 만든다

```
python render.py <날짜>     # docs/<날짜>.html
python render.py --all      # 기사가 있는 날 전부 + index.html
```

- 화면 구조와 CSS 는 `docs/ui/d-merged.html` 에서 가져온다. 새로 디자인하지 않는다
- **예시임을 알리던 노란 띠를 걷어낸다** — 이제 진짜 데이터다
- `docs/index.html` 은 가장 최근 날짜로 덮어쓴다
- 하단에 최근 날짜 링크 줄 (최대 14개). 없으면 67일치에 닿을 길이 없다

### 3. 로컬 실행 → 커밋 → 배포 확인

```
python fetch.py && python render.py --all
git add docs && git commit && git push
```

Vercel 이 약 16초 뒤 발행한다 (실측).

---

## 명세 — 코드 상수가 될 것들

> **코드에 상수로 박고 여기는 참고용으로만 둔다.** 두 곳에 적으면 반드시 어긋난다.
> 늘릴 때는 먼저 질문한다 (`CLAUDE.md`).

**카테고리 7 + 미분류** — 매일 같아야 어제와 비교가 된다. 애매하면 억지로 넣지 않고 `미분류`.
`패키징` `파운드리·공정` `메모리` `AI·가속기` `장비·소재` `정책·규제` `투자·실적` `미분류`

> 패키징은 원래 `파운드리·공정` 에 있었는데 67일치에서 48% 를 차지해 분리했다 (2026-08-22).
> 수집처 8곳 중 2곳이 패키징 전문이다.

**선별 점수** — `[공식]` +3 · 관심 기업 언급 +1. 동점이면 최신순.
카테고리당 최대 3건, 자리가 남으면 점수 순으로 채운다.

> `묶음 크기 +1` 과 `기존 사건 후속 +2` 는 뺐다. 묶음은 중복 묶기가 있어야 생기고,
> `후속` 은 선별 뒤에 도는 연결이 만드는 값이라 **순환한다.** 둘 다 생긴 뒤에 되살린다.

**고유 열쇠** — 원문 링크. 같은 기사를 두 번 처리하지 않는다.

**표기** — 모든 기사에 발행시각·수집시각·원문 링크. 모르면 `—`.

**출처 등급** — `[공식]`(기업 발표) / `[2차]`(언론). `data/sources.md` 에 적혀 있다.

---

## 화면

```
2026-08-20  ·  10건  ·  수집 8곳 중 8곳 정상

[전체 10] [패키징 3] [파운드리·공정 3] [메모리 2] [AI·가속기 1] [장비·소재 1] [메모리 0] …

┌─ 패키징                                          [2차]
│  Nordson Test & Inspection Opens Center of Excellence
│  (요약 두 줄)
│  더일렉 · 발행 08-20 06:26 · 수집 08-22 20:10 · 원문 ↗
└─
```

- 상단 **필터 칩에 카테고리별 건수**를 둔다 (`메모리 0`).
  기사가 4건인 날에도 빈 섹션 6개가 생기지 않게 하는 장치다. **이번엔 표시만 하고 동작하지 않는다**
- 카드에 카테고리 태그와 출처 등급 배지를 함께
- `미분류` 도 그대로 보여준다. 숨기지 않는다
- 기사가 0건인 날 — "이 날은 반도체 관련 기사가 없습니다".
  **수집 실패와 구분해서** 적는다
- 하단에 `투자 조언이 아닙니다` 와 출처 파일 표기

---

## 검증

| # | 명령 | 통과 기준 |
| --- | --- | --- |
| 1 | `python select.py 2026-08-20` | 16건 → 10건, 카테고리당 3건 이하, **자른 6건**이 출력에 보인다 |
| 2 | `python select.py 2026-08-22` | **4건 그대로** (상한 미만), 자른 0건 |
| 3 | `python render.py --all` | `docs/` 에 날짜별 HTML 과 `index.html` 생성 |
| 4 | 브라우저에서 `docs/index.html` | 카드·태그·배지·원문 링크가 보인다 |
| 5 | `curl -s https://semi-program.vercel.app \| grep -c thelec` | **0 보다 크다** (예시가 아니라 실제 기사) |
| 6 | `docs/2026-08-15.html` | '기사 없음' 을 적는다. 빈 페이지가 아니다 |

## 이번에 안 하는 것

`extract.py`(모델 분류·요약) · `link.py`(이어지는 흐름) · `cluster.py`(중복 묶기) ·
GitHub Actions · 개념 탭 · 필터 칩 동작 · 기업명 정규화.

전부 다음 세션이다. 자세한 이유는 `WBS.md` 3절.
