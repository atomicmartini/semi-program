# PLAN — 명세와 화면

> 왜·누구·무엇은 `PRD.md`, 규칙은 `CLAUDE.md`, **작업 쪼갬과 일정은 `WBS.md`**.
> 이 문서는 만들 때 참고할 값과 화면 모양만 담는다.

## 명세 — 코드 상수가 될 것들

> `CLAUDE.md` 에서 내려온 것. **코드에 상수로 박고 여기는 참고용으로만 둔다.**
> 두 곳에 적으면 반드시 어긋난다. 늘릴 때는 먼저 질문한다.

**카테고리 7 + 미분류** — 매일 같아야 비교가 된다. 애매하면 억지로 넣지 않고 `미분류`.
`패키징` `파운드리·공정` `메모리` `AI·가속기` `장비·소재` `정책·규제` `투자·실적` `미분류`

> 패키징은 원래 `파운드리·공정` 에 넣었다가 67일치에서 48% 를 차지해 분리했다 (2026-08-22).
> 수집처 8곳 중 2곳이 패키징 전문이다.

**관계 종류 10** — 열린 집합으로 두면 집계가 안 된다.
`공급` `투자` `인수` `제휴` `소송` `증설` `감산` `수주` `규제` `인력이동`

**관계 상태 3** — `확정`(`[공식]` 출처 있음) / `보도됨`(`[2차]`만) / `확인 필요`

**10건 선별 점수** — `[공식]` +3 · 관심 기업 +1
동점이면 최신순. **카테고리당 최대 3건**, 10건 안 차면 점수 순으로 채운다.

> 원래 여기에 `묶음 크기 +1`, `기존 사건 후속 +2` 도 있었으나 뺐다.
> 묶음은 중복 묶기가 있어야 생기는데 이번 범위 밖이고,
> `후속 여부` 는 선별(세션 2) 뒤에 도는 연결(세션 3) 이 만드는 값이라 **순환한다.**
> 둘 다 생긴 뒤에 되살린다.

**기사 연결 판정** — 공통 기업 수 · 시간 거리 · 키워드 겹침으로 점수.
임계값 아래는 잇지 않고 `확인 필요`. **왜 이었는지 점수와 함께 남긴다.**

**고유 열쇠** — 원문 링크. 같은 기사를 두 번 처리하지 않는다.

**표기** — 모든 사실에 발행시각·수집시각·원문 링크. 모르면 `—`.

## 화면 설계

### 오늘 페이지 (기본)

```
2026-08-22  ·  10건  ·  수집 8곳 중 8곳 정상

┌─ [메모리]                                        보도됨
│  SK하이닉스, HBM4 양산 준비 착수
│  (한국어 요약 두 줄)
│
│  ↳ 이어지는 흐름 3건                          [펼치기]
│      08-05  SK하이닉스 HBM4 샘플 공급
│      07-22  엔비디아 HBM4 공급사 선정 착수
│  달라진 점: 07-22 엔 '검토'였고 오늘은 '양산 준비'다
│
│  원문 → thelec.kr  ·  발행 08-22 09:14  ·  수집 08-22 18:00
└─
```

- 카테고리 태그 + 상태 배지(`확정`/`보도됨`/`확인 필요`)를 항상 함께
- **"달라진 점" 한 줄이 이 제품의 정체성.** 없으면 그 줄을 안 쓴다
- 이어지는 흐름이 없으면 그 영역을 아예 안 보여준다
- 하단에 **"투자 조언이 아닙니다"** 항상 표시

### 이어지는 흐름 — 카드 안에서 펼친다

'이어지는 흐름 N건' 을 누르면 **카드 안에서** 세로 시간축이 펼쳐진다.
별도 페이지를 만들지 않는다. 실제 모양은 `docs/index.html` 참조.

## 아직 안 정한 것

| # | 정할 것 | 언제까지 |
| --- | --- | --- |
| Q6 | 카테고리당 3건이 맞는가 | 세션 2, 실제로 돌려 보고 |
| Q7 | 매일 몇 시에 돌릴 것인가 | 세션 3, Actions 만들 때 |
| Q8 | 용어 사전 초기 항목 몇 개로 시작 | 개념 탭을 만들 때 (이번 범위 밖) |
| Q9 | **보여줄 취준생 1명이 누구인가** | 세션 3 전까지 |

## 첫 며칠 문제

데이터가 없으면 연결이 아무것도 못 한다. `WBS.md` 의 `3.2 backfill` 이 이걸 푼다.
각 피드가 주는 과거분을 첫 실행 때 한 번에 받아 쌓는다 (Semiconductor Digest 는 100건을 준다).
