# PLAN — 2026-08-22

> 왜·누구는 `PRD.md`, 규칙은 `CLAUDE.md`, 전체 작업 쪼갬은 `WBS.md`.
> 지난 판은 `PLAN_pre.md` 에 보관 — 아카이브이므로 참조하지 않는다.

## 오늘의 성공

> **배포된 URL 에서 뉴스가 보이고, 어떤 개념의 단어를 클릭하면 개념 탭으로 넘어간다.**

## 스켈레톤 — 제일 먼저 뚫을 최소 경로

```
RSS 8곳 → 오늘 기사 → 뉴스 HTML ─(용어 클릭)→ 개념 HTML → 배포된 URL
         [collect]    [pick][publish]           [concept]
          이미 뚫림          오늘 뚫을 곳
```

슬라이스 경계와 이름을 지금 정한다. **폴더로 나누는 것은 파일이 6개를 넘으면 한다.**

| 슬라이스 | 하는 일 | 파일 |
| --- | --- | --- |
| `collect` | 모으고 거른다 | `fetch.py` `filter.py` (있음) |
| `pick` | 무엇을 보여줄지 고른다 | `select.py` (검증 필요) |
| `publish` | 화면으로 낸다 | `render.py` (오늘) |
| `concept` | 용어를 설명한다 | `data/glossary.md` + 개념 페이지 (오늘) |

## 오늘 만들 기능

1. 사용자가 **배포된 URL 에서 오늘 반도체 뉴스를 카테고리별로 볼 수 있다**
2. 사용자가 **기사 속 용어를 눌러 그 뜻과 출처를 볼 수 있다**
3. 사용자가 **카테고리 칩을 눌러 그 분야만 걸러 볼 수 있다**

덤 — 하단에 지난 날짜 링크 한 줄. 오늘 날짜는 4건뿐이라 16건인 날을 보여줄 길이 필요하다.

## 완료 조건 — 눈으로 확인 가능한 것만

**기능 1**
- `semi-program.vercel.app` 을 열면 기사 카드가 보인다
- 카드마다 카테고리 태그·출처 등급 배지·원문 링크가 있다
- 상단에 `N건` 과 `수집 8곳 중 N곳 정상` 이 보인다

**기능 2**
- 기사 요약 속 용어에 점선 밑줄이 보인다
- 누르면 개념 페이지의 그 항목으로 이동한다
- 그 항목에 **출처 링크와 확인일**이 보인다

**기능 3**
- 상단 칩에 카테고리별 건수가 보인다 (`메모리 0`)
- 칩을 누르면 그 카테고리 카드만 남는다
- `전체` 를 누르면 도로 다 보인다

## 오늘 안 만들 것 — 전부 필요하다. **오늘만** 안 한다

| 안 할 것 | 언제 | 없으면 오늘 이렇게 보인다 |
| --- | --- | --- |
| 모델 호출 (`extract.py`) | **내일 첫 작업** | 영문 기사가 영문 그대로. 미분류 19% |
| 이어지는 흐름 (`link.py`) | 내일 (모델 호출이 먼저) | 카드에 '이어지는 흐름' 영역이 없다 |
| 중복 묶기 (`cluster.py`) | 그 다음 | 비슷한 기사가 따로따로 보일 수 있다 |
| GitHub Actions 자동 실행 | 흐름이 다 돈 뒤 | 내일 뉴스가 자동으로 안 올라온다. 손으로 돌린다 |
| 기업명 정규화 | 연결 기능이 생긴 뒤 | 오늘은 티가 안 난다 (화면에 기업명을 안 쓴다) |
| 슬라이스 폴더 나누기 | 파일이 6개를 넘으면 | 파일이 루트에 평평하게 있다 |
| 용어 자동 수집 | 개념 탭이 자리 잡은 뒤 | 사전이 저절로 안 늘어난다. 오늘 넣은 5개가 전부 |
| 원문 전체 수집·번역 | 안 함 (`PRD.md`) | 요약과 링크만 |

## 걱정되는 것

> **용어 5개의 출처를 오늘 안에 확보할 수 있는가.**

`CLAUDE.md` 규칙 — *개념 설명은 출처와 확인일 없이 싣지 않는다.*
지금 `data/glossary.md` 의 용어 3개는 전부 `출처 (빈칸 — 확인 안 됨)` 상태다.
**출처를 못 붙이면 기능 2가 통째로 막힌다.**

완화책 — 3개만이라도 확보하고 나머지는 `(빈칸 — 준비 중)` 으로 두고 링크를 안 건다.

## 검증

| # | 명령 | 통과 기준 |
| --- | --- | --- |
| 1 | `python select.py 2026-08-20` | 16건 → 10건, 카테고리당 3건 이하, **자른 6건**이 출력에 보인다 |
| 2 | `python select.py 2026-08-22` | 4건 그대로 (상한 미만), 자른 0건 |
| 3 | `python render.py --all` | `docs/` 에 날짜별 HTML · `index.html` · `concepts.html` 생성 |
| 4 | `curl -s https://semi-program.vercel.app \| grep -c thelec` | **0 보다 크다** (예시가 아니라 실제 기사) |
| 5 | 브라우저에서 용어 클릭 | 개념 페이지 해당 항목으로 이동하고 출처·확인일이 보인다 |
| 6 | 브라우저에서 칩 클릭 | 그 카테고리 카드만 남는다 |
| 7 | `docs/2026-08-15.html` | '기사 없음' 을 적는다. 수집 실패와 구분한다 |
