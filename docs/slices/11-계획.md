# 이어지는 흐름 — 모델 판정 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이어지는 흐름을 키워드 겹침이 아니라 기사 내용으로 판정한다 — 코드가 TF-IDF 로 후보를 좁히고, 모델이 그중 정말 이어지는 것만 고르고, 근거 인용구를 코드가 원문과 대조해 검사한다.

**Architecture:** `link.py` 를 다시 쓴다. 후보 추리기(`shortlist`)는 네트워크를 타지 않는 순수 함수라 테스트가 쉽고, 나중에 임베딩으로 갈아끼울 자리다. 판정(`judge`)은 모델을 부르되 호출 함수를 인자로 받아 테스트에서 가짜로 바꿔 끼운다. 인용구 대조(`verify_links`)도 순수 함수다. **세 함수 모두 따로 테스트되고, 모델 없이 돌아가는 부분이 대부분이다.**

**Tech Stack:** 파이썬 표준 라이브러리만. 모델은 OpenRouter `nvidia/nemotron-3-super-120b-a12b:free` (`extract.py` 와 같은 것). **새 라이브러리 없음.**

**Spec:** `docs/slices/11-이어지는흐름-모델판정.md`

## Global Constraints

- **의존성 0개.** 표준 라이브러리만 쓴다. 새 라이브러리를 추가하지 않는다 (`CLAUDE.md`)
- **랭체인·랭그래프 안 쓴다** (`CLAUDE.md`)
- **인용구 없는 관계는 쓰지 않는다.** 원문에 없는 인용구가 붙은 연결은 코드가 버린다 (`CLAUDE.md`)
- **수집·판정 실패를 조용히 넘기지 않는다.** 실패는 `link_error` 에 남기고 실행 출력에 적는다
- **억지로 잇지 않는다.** 모델이 하나도 안 고르면 '이어지는 흐름 없음'
- 후보 15건 · 프롬프트에 넣는 요약 앞 300자 · 한 기사당 흐름 상한 10건 (`MAX_RELATED`)
- 커밋은 기능 단위 하나씩. 각 Task 끝에 커밋한다
- 완료 판정은 `python -m unittest discover -p "test_*.py"` 를 실제로 실행해 확인한다

---

### Task 1: TF-IDF 후보 추리기

기업·키워드 조건을 버리고, 제목+요약 전체를 TF-IDF 코사인으로 재서 상위 N 건을 후보로 준다.
네트워크를 타지 않는다.

**Files:**
- Modify: `link.py` (전면 재작성 시작 — `find_related`·`bridged`·`topic_keywords`·`read_topic_keywords`·`read_relation_pairs` 삭제)
- Modify: `test_link.py:1-120` (옛 기준 테스트 삭제, 새 테스트로 교체)

**Interfaces:**
- Consumes: `filter.filter_articles`, `link._load_all_past` (기존 그대로 둔다)
- Produces:
  - `tokens(text: str) -> list[str]`
  - `shortlist(article: dict, past: list[dict], limit: int = 15) -> list[dict]` — 점수 내림차순, 원소는 `past` 의 기사 dict 그대로

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_link.py` 의 `TestTopicKeywords`·`TestBridged`·`TestFindRelated` 클래스와
`from link import bridged, find_related, topic_keywords` 를 지우고, 아래를 넣는다.
(`TestCompaniesMentioned` 는 `companies.py` 를 검사하는 것이라 그대로 둔다.)

```python
from link import shortlist, tokens


class TestTokens(unittest.TestCase):
    def test_splits_korean_and_english(self):
        got = tokens("HBM 패키징 co-packaged optics")
        self.assertIn("hbm", got)
        self.assertIn("패키징", got)
        self.assertIn("co-packaged", got)

    def test_ignores_single_letter_noise(self):
        self.assertEqual(tokens("a b c"), [])


class TestShortlist(unittest.TestCase):
    """후보 추리기는 '놓치지 않는 일'만 한다. 가려내는 것은 모델이 한다."""

    def _article(self, title, summary, published="2026-08-20"):
        return {"url": title, "title": title, "summary": summary, "published": published}

    def test_rare_word_beats_common_word(self):
        # 지금 문제 그 자체 — '메모리'만 겹치는 기사가 CPO 기사를 밀어내면 안 된다
        today = self._article("CPO 청사진", "co-packaged optics 로 메모리를 빛으로 잇는다")
        cpo = self._article("GlobalFoundries co-packaged optics", "co-packaged optics 채택", "2026-05-05")
        common = self._article("DDR4 가격", "메모리 가격이 올랐다", "2026-07-01")
        filler = [self._article(f"메모리 소식 {i}", "메모리 이야기", "2026-06-01") for i in range(8)]
        got = shortlist(today, [common, cpo, *filler], limit=15)
        self.assertEqual(got[0]["url"], cpo["url"])

    def test_respects_limit(self):
        today = self._article("HBM", "HBM 이야기")
        past = [self._article(f"HBM {i}", "HBM 이야기", "2026-07-01") for i in range(30)]
        self.assertEqual(len(shortlist(today, past, limit=15)), 15)

    def test_empty_past_gives_empty_list(self):
        self.assertEqual(shortlist(self._article("제목", "요약"), [], limit=15), [])
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m unittest test_link -v`
Expected: FAIL — `ImportError: cannot import name 'shortlist' from 'link'`

- [ ] **Step 3: 최소 구현**

`link.py` 의 맨 위 독스트링을 아래로 바꾸고, `read_topic_keywords`·`topic_keywords`·
`read_relation_pairs`·`bridged`·`find_related` 를 지운 자리에 넣는다.
`companies` 임포트와 `MIN_KEYWORDS` 도 함께 지운다.

```python
"""오늘 기사와 정말 이어지는 과거 기사를 잇는다.

    후보 = TF-IDF 상위 15건        (코드 · 놓치지 않는 일만 한다)
    이어짐 = 모델이 고른 것 AND 근거 인용구가 원문에 실제로 있다

후보 추리기와 판정을 나눈 이유는 docs/slices/11-이어지는흐름-모델판정.md 에 있다 —
유사도 지표는 '놓치지 않기'는 잘하고 '가려내기'는 못한다. 네 번 재보고 확인했다.
억지로 잇지 않는다 — 모델이 하나도 안 고르면 '이어지는 흐름 없음'.
"""

import json
import math
import re
import sys
import time
from collections import Counter
from pathlib import Path

from filter import filter_articles

HERE = Path(__file__).parent
ARTICLE_DIR = HERE / "data" / "articles"

MAX_RELATED = 10      # 기사 하나당 화면에 보여줄 최대 건수. 사용자가 정한 값
SHORTLIST = 15        # 모델에게 넘길 후보 수. 늘리면 프롬프트가 길어진다
SUMMARY_CHARS = 300   # 프롬프트에 넣는 요약 길이. 더 자르면 판정 근거가 얇아진다

# 한글은 2글자 이상 덩어리, 영문·숫자는 낱말 단위. 한 글자는 증거가 못 된다.
_TOKEN = re.compile(r"[가-힣]{2,}|[a-zA-Z][a-zA-Z0-9\-]{1,}")


def tokens(text: str) -> list[str]:
    """TF-IDF 로 셀 말들. 한국어는 조사가 붙어('광연결로') 앞 2~3글자도 함께 본다."""
    out: list[str] = []
    for t in _TOKEN.findall(text.lower()):
        out.append(t)
        if not t.isascii() and len(t) > 2:
            out.extend(t[:n] for n in (2, 3) if len(t) > n)
    return out


def _vector(toks: list[str], idf: dict[str, float], default_idf: float) -> dict[str, float]:
    tf = Counter(toks)
    vec = {w: (1 + math.log(c)) * idf.get(w, default_idf) for w, c in tf.items()}
    norm = math.sqrt(sum(x * x for x in vec.values())) or 1.0
    return {w: x / norm for w, x in vec.items()}


def shortlist(article: dict, past: list[dict], limit: int = SHORTLIST) -> list[dict]:
    """모델에게 넘길 후보를 고른다. 판정이 아니라 '놓치지 않기' 다.

    흔한 말(`메모리`)은 문서빈도가 높아 가중치가 저절로 낮아진다 — 옛 기준이
    `메모리` 하나로 이어 버리던 문제를 수식이 대신 막는다.
    """
    if not past:
        return []

    docs = [tokens(f"{a['title']} {a['summary']}") for a in past]
    n = len(docs)
    df: Counter[str] = Counter()
    for d in docs:
        df.update(set(d))
    idf = {w: math.log(n / (1 + c)) for w, c in df.items()}
    default_idf = math.log(n) if n else 0.0

    today_vec = _vector(tokens(f"{article['title']} {article['summary']}"), idf, default_idf)
    scored = []
    for a, d in zip(past, docs):
        vec = _vector(d, idf, default_idf)
        score = sum(today_vec[w] * vec[w] for w in today_vec.keys() & vec.keys())
        if score > 0:
            scored.append((score, a))
    scored.sort(key=lambda x: -x[0])
    return [a for _, a in scored[:limit]]
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m unittest test_link -v`
Expected: PASS (이 시점에 `link_day` 는 아직 깨져 있다 — Task 3 에서 고친다)

- [ ] **Step 5: 커밋**

```bash
git add link.py test_link.py
git commit -m "feat: 이어지는 흐름 후보를 TF-IDF 로 추린다"
```

---

### Task 2: 모델 답 해석과 인용구 대조

모델이 준 답에서 연결을 읽고, **인용구가 과거 기사 원문에 실제로 있는 것만** 남긴다.
네트워크를 타지 않는다.

**Files:**
- Modify: `link.py`
- Modify: `test_link.py`

**Interfaces:**
- Consumes: `extract._parse_json(text) -> dict | None`, `extract._verify_quote(quote, haystack) -> bool`, Task 1 의 `shortlist`
- Produces: `verify_links(parsed: dict | None, candidates: list[dict], limit: int = MAX_RELATED) -> list[dict]`
  — 원소는 `{"date": str, "title": str, "url": str, "reason": str, "quote": str}`, 최신순

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_link.py` 에 추가한다.

```python
from link import verify_links


class TestVerifyLinks(unittest.TestCase):
    """지어낸 연결을 막는 방어선. 인용구가 원문에 없으면 그 연결은 버린다 (CLAUDE.md)."""

    def _candidate(self, url, summary, published="2026-05-05"):
        return {"url": url, "title": f"제목 {url}", "summary": summary, "published": published}

    def test_keeps_link_whose_quote_is_in_the_source(self):
        cands = [self._candidate("a", "GlobalFoundries 가 co-packaged optics 채택을 가속한다")]
        parsed = {"links": [{"url": "a", "reason": "같은 CPO 채택 흐름", "quote": "co-packaged optics 채택을 가속"}]}
        got = verify_links(parsed, cands)
        self.assertEqual(len(got), 1)
        self.assertEqual(got[0]["reason"], "같은 CPO 채택 흐름")

    def test_drops_link_whose_quote_is_invented(self):
        cands = [self._candidate("a", "GlobalFoundries 가 co-packaged optics 채택을 가속한다")]
        parsed = {"links": [{"url": "a", "reason": "그럴듯한 이유", "quote": "원문에 없는 문장이다"}]}
        self.assertEqual(verify_links(parsed, cands), [])

    def test_drops_link_to_url_not_in_candidates(self):
        cands = [self._candidate("a", "본문")]
        parsed = {"links": [{"url": "없는주소", "reason": "이유", "quote": "본문"}]}
        self.assertEqual(verify_links(parsed, cands), [])

    def test_broken_answer_gives_empty_list(self):
        self.assertEqual(verify_links(None, [self._candidate("a", "본문")]), [])

    def test_respects_limit(self):
        cands = [self._candidate(str(i), "같은 본문 문장") for i in range(15)]
        parsed = {"links": [{"url": str(i), "reason": "이유", "quote": "같은 본문"} for i in range(15)]}
        self.assertEqual(len(verify_links(parsed, cands, limit=10)), 10)
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m unittest test_link -v`
Expected: FAIL — `ImportError: cannot import name 'verify_links'`

- [ ] **Step 3: 최소 구현**

`link.py` 에 넣는다. `extract.py` 의 밑줄 이름을 그대로 가져다 쓴다 —
같은 일을 두 번 구현하면 한쪽만 고쳐져 어긋난다.

```python
from extract import _parse_json, _verify_quote


def verify_links(parsed: dict | None, candidates: list[dict], limit: int = MAX_RELATED) -> list[dict]:
    """모델이 고른 연결 중 근거가 진짜인 것만 남긴다.

    인용구가 그 과거 기사 원문에 실제로 있는지 코드가 대조한다.
    없으면 그 연결만 버린다 — 지어낸 연결을 화면에 내지 않는다 (CLAUDE.md).
    """
    if not parsed:
        return []

    by_url = {c["url"]: c for c in candidates}
    out: list[dict] = []
    for item in parsed.get("links") or []:
        past = by_url.get((item.get("url") or "").strip())
        if past is None:
            continue  # 후보에 없던 주소를 지어낸 경우
        quote = (item.get("quote") or "").strip()
        if not _verify_quote(quote, f"{past['title']} {past['summary']}"):
            continue
        out.append(
            {
                "date": (past.get("published") or "")[:10],
                "title": past["title"],
                "url": past["url"],
                "reason": (item.get("reason") or "").strip(),
                "quote": quote,
            }
        )

    out.sort(key=lambda r: r["date"], reverse=True)
    return out[:limit]
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m unittest test_link -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add link.py test_link.py
git commit -m "feat: 이어지는 흐름의 인용구를 원문과 대조해 검사한다"
```

---

### Task 3: 모델 판정과 하루치 조립

후보를 모델에 한 번 넘겨 판정받고, 하루치를 조립해 저장한다.
모델 호출 함수를 인자로 받아 **테스트에서는 가짜를 끼운다.**

**Files:**
- Modify: `link.py` (`link_day` 재작성, `judge`·`build_prompt` 추가)
- Modify: `test_link.py`

**Interfaces:**
- Consumes: Task 1 `shortlist`, Task 2 `verify_links`, `extract._call_model(prompt, key)`, `extract._load_key()`, `extract.should_retry(exc)`, `extract.ModelError`, `extract.RETRIES`, `extract.BACKOFF`, `extract.PAUSE`
- Produces:
  - `build_prompt(article: dict, candidates: list[dict]) -> str`
  - `judge(article, candidates, key: str, call=None) -> tuple[list[dict], str | None]` — (연결 목록, 오류 원문 또는 None). `call` 은 `(prompt, key) -> str`
  - `link_day(day: str) -> list[dict]` — 각 원소에 `related` 와, 실패한 경우 `link_error`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
from link import build_prompt, judge


class TestBuildPrompt(unittest.TestCase):
    def test_includes_candidate_urls_and_trims_summary(self):
        today = {"url": "t", "title": "오늘", "summary": "오늘 요약", "published": "2026-08-20"}
        cands = [{"url": "a", "title": "과거", "summary": "가" * 500, "published": "2026-05-05"}]
        prompt = build_prompt(today, cands)
        self.assertIn("a", prompt)
        self.assertIn("오늘", prompt)
        self.assertNotIn("가" * 400, prompt)   # 앞 300자만 넣는다


class TestJudge(unittest.TestCase):
    """모델을 진짜로 부르지 않는다 — 호출 함수를 바꿔 끼운다."""

    def setUp(self):
        self.cands = [{"url": "a", "title": "과거", "summary": "co-packaged optics 채택", "published": "2026-05-05"}]
        self.today = {"url": "t", "title": "오늘", "summary": "CPO 청사진", "published": "2026-08-20"}

    def test_returns_verified_links_on_good_answer(self):
        answer = '{"links": [{"url": "a", "reason": "같은 CPO 흐름", "quote": "co-packaged optics 채택"}]}'
        links, err = judge(self.today, self.cands, "열쇠", call=lambda p, k: answer)
        self.assertIsNone(err)
        self.assertEqual(len(links), 1)

    def test_reports_error_when_model_fails(self):
        def boom(prompt, key):
            raise RuntimeError("모델이 죽었다")
        links, err = judge(self.today, self.cands, "열쇠", call=boom)
        self.assertEqual(links, [])
        self.assertIn("모델이 죽었다", err)   # 실패를 조용히 넘기지 않는다 (CLAUDE.md)

    def test_broken_json_is_an_error_not_a_crash(self):
        links, err = judge(self.today, self.cands, "열쇠", call=lambda p, k: "말이 안 되는 답")
        self.assertEqual(links, [])
        self.assertIsNotNone(err)

    def test_no_candidates_means_no_model_call(self):
        called = []
        links, err = judge(self.today, [], "열쇠", call=lambda p, k: called.append(1) or "{}")
        self.assertEqual((links, err, called), ([], None, []))
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m unittest test_link -v`
Expected: FAIL — `ImportError: cannot import name 'build_prompt'`

- [ ] **Step 3: 최소 구현**

```python
from extract import BACKOFF, PAUSE, RETRIES, _call_model, _load_key, should_retry

PROMPT = """오늘 기사와 정말로 이어지는 과거 기사만 고르라. 다른 말은 하지 말고 JSON 으로만 답하라.

이어진다 = 같은 사안·같은 기술 흐름이 실제로 이어진다는 뜻이다.
같은 회사가 나온다거나 같은 분야라는 이유로 고르지 마라. 그건 이어지는 것이 아니다.
하나도 없으면 links 를 빈 배열로 둬라. 억지로 고르지 마라.
quote 는 그 과거 기사 본문에서 **그대로** 옮긴다. 지어내면 코드가 대조해서 버린다.

{{"links": [{{"url": "과거 기사 주소", "reason": "왜 이어지는지 한 줄", "quote": "과거 기사 원문 문장"}}]}}

[오늘 기사]
제목: {title}
본문: {body}

[과거 기사 후보]
{candidates}
"""


def build_prompt(article: dict, candidates: list[dict]) -> str:
    lines = []
    for c in candidates:
        date = (c.get("published") or "")[:10]
        lines.append(f"- url: {c['url']}\n  날짜: {date}\n  제목: {c['title']}\n  본문: {c['summary'][:SUMMARY_CHARS]}")
    return PROMPT.format(
        title=article["title"],
        body=article["summary"][:SUMMARY_CHARS],
        candidates="\n".join(lines),
    )


def judge(article: dict, candidates: list[dict], key: str, call=None) -> tuple[list[dict], str | None]:
    """후보를 모델에 한 번 넘겨 판정받는다. (연결 목록, 오류 원문)

    실패를 삼키지 않는다 — 실패하면 흐름을 비우고 이유를 돌려준다 (CLAUDE.md).
    """
    if not candidates:
        return [], None

    call = call or _call_model
    prompt = build_prompt(article, candidates)

    for attempt in range(RETRIES):
        try:
            raw = call(prompt, key)
            break
        except Exception as e:  # noqa: BLE001 — 어떤 실패든 그 기사만 비우고 넘어간다
            if attempt == RETRIES - 1 or not should_retry(e):
                return [], f"{type(e).__name__} {e}"
            time.sleep(BACKOFF * (attempt + 1))

    parsed = _parse_json(raw)
    if parsed is None:
        return [], "모델 출력을 JSON 으로 못 읽음"
    return verify_links(parsed, candidates), None
```

그리고 `link_day` 를 통째로 바꾼다.

```python
def link_day(day: str) -> list[dict]:
    """오늘 선별된 기사마다 이어지는 흐름을 붙인다."""
    from pick import select_day

    picked, _, _ = select_day(day)
    past = _load_all_past(day)
    key = _load_key()

    linked = []
    for a in picked:
        candidates = shortlist(a, past)
        related, error = judge(a, candidates, key)
        entry = {**a, "related": related}
        if error:
            entry["link_error"] = error
        linked.append(entry)
        time.sleep(PAUSE)  # 몰아치면 429 가 난다
    return linked
```

`main` 의 출력에 실패를 드러내는 줄을 더한다 (`with_flow` 를 세는 줄 바로 뒤).

```python
    failed = [a for a in linked if a.get("link_error")]
    for a in failed:
        # 판정 실패를 조용히 넘기지 않는다 (CLAUDE.md).
        print(f"  ! {a['title'][:40]} — 판정 실패: {a['link_error']}", file=sys.stderr)
```

그리고 흐름을 찍는 줄에 이유를 함께 낸다.

```python
    for a in with_flow:
        print(f"  · {a['title']}")
        for r in a["related"]:
            print(f"      → {r['date']} {r['title']}")
            print(f"        이유: {r['reason']}")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m unittest discover -p "test_*.py"`
Expected: PASS — 전부

- [ ] **Step 5: 진짜 데이터로 한 번 돌려본다**

Run: `python link.py 2026-08-20`
Expected: CPO 기사 흐름에서 '슈퍼 모멘텀 출간'·'용인 클린룸'·'DDR4 가격'이 사라진다.
남은 연결마다 '이유' 줄이 보인다. **여기서 결과가 이상하면 멈추고 사용자에게 알린다.**

프롬프트가 길어 모델이 한도 오류(`context`·`too long`·400)를 내면 **`SHORTLIST` 를 10 으로 줄인다.**
`SUMMARY_CHARS` 는 더 줄이지 않는다 — 판정 근거가 얇아진다 (명세 '정하고 가는 값').
같은 오류가 두 번 나면 고치지 말고 멈추고, 시도한 것·오류 원문·다음 후보 2개를 적어 묻는다 (`CLAUDE.md`).

- [ ] **Step 6: 커밋**

```bash
git add link.py test_link.py
git commit -m "feat: 이어지는 흐름을 모델이 판정한다"
```

---

### Task 4: 화면에 이유와 인용구를 보여준다

지금 흐름은 날짜·제목만 나온다. 저장만 하고 안 보여주던 근거를 드러낸다.

**Files:**
- Modify: `render.py` (`STEP` 템플릿, `render_thread`, `STYLE`)
- Modify: `test_render.py`

**Interfaces:**
- Consumes: Task 2 가 저장한 `related[]` 항목의 `reason`·`quote`
- Produces: 없음 (화면만 바뀐다)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_render.py` 에 추가한다.

```python
class TestRenderThreadEvidence(unittest.TestCase):
    """왜 이어지는지를 화면에 드러낸다 — 저장만 하고 안 보여주면 가른 의미가 없다."""

    def _related(self, **kw):
        base = {"date": "2026-05-05", "title": "과거 기사", "url": "http://a",
                "reason": "같은 CPO 채택 흐름", "quote": "co-packaged optics 채택을 가속"}
        return [{**base, **kw}]

    def test_shows_reason_and_quote(self):
        html = render_thread(self._related())
        self.assertIn("같은 CPO 채택 흐름", html)
        self.assertIn("co-packaged optics 채택을 가속", html)

    def test_survives_old_data_without_reason(self):
        # 옛 .linked.json 에는 reason·quote 가 없다. 깨지지 않아야 한다
        html = render_thread([{"date": "2026-05-05", "title": "과거", "url": "http://a"}])
        self.assertIn("과거", html)

    def test_escapes_html_in_quote(self):
        html = render_thread(self._related(quote="<script>x</script>"))
        self.assertNotIn("<script>", html)
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m unittest test_render -v`
Expected: FAIL — `AssertionError: '같은 CPO 채택 흐름' not found`

- [ ] **Step 3: 최소 구현**

`render.py` 의 `STEP` 을 바꾼다.

```python
STEP = """<div class="step">
  <span class="when">{when}</span>
  <div class="what"><a href="{url}">{title}</a>{why}</div>
</div>
"""

WHY = """<div class="why">{reason}</div><blockquote class="q">{quote}</blockquote>"""
```

`render_thread` 안의 `STEP.format(...)` 을 바꾼다.

```python
    steps = "".join(
        STEP.format(
            when=html.escape(r["date"][5:] or r["date"]),
            title=html.escape(r["title"]),
            url=html.escape(r["url"], quote=True),
            # 옛 .linked.json 에는 근거가 없다. 없으면 줄을 안 낸다
            why=(
                WHY.format(reason=html.escape(r["reason"]), quote=html.escape(r["quote"]))
                if r.get("reason") and r.get("quote")
                else ""
            ),
        )
        for r in oldest_first
    )
```

`STYLE` 의 `.step .when` 규칙 바로 뒤에 넣는다.

```css
  .step .why { font-size:12px; color:var(--text-muted); margin-top:3px; }
  .step .q { margin:4px 0 0; padding-left:9px; border-left:2px solid var(--border);
    font-size:12px; color:var(--text-faint); }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m unittest discover -p "test_*.py"`
Expected: PASS — 전부

- [ ] **Step 5: 커밋**

```bash
git add render.py test_render.py
git commit -m "feat: 이어지는 흐름에 이유와 인용구를 보여준다"
```

---

### Task 5: `CLAUDE.md` 규칙 개정

연결 판정을 모델에게 넘겼으므로 규칙 문장이 코드와 어긋난다. **코드와 규칙이 다른 상태로 두지 않는다.**

**Files:**
- Modify: `CLAUDE.md:29` (`# 규칙` 절)

**Interfaces:**
- Consumes: 없음
- Produces: 없음

- [ ] **Step 1: 규칙 문장을 고친다**

지금 줄:

```
- **점수 매기기·판정·기업명 정규화는 코드가 한다.** 모델은 분류·요약·관계 추출만 한다
```

이렇게 바꾼다.

```
- **점수 매기기·기업명 정규화는 코드가 한다.** 모델은 분류·요약·관계 추출, 그리고 이어지는 흐름 판정을 한다
- **모델이 판정한 연결은 코드가 검사한다** — 근거 인용구가 원문에 실제로 있는지 대조하고,
  통과 못 한 연결은 버린다 (`docs/slices/11-이어지는흐름-모델판정.md`)
```

- [ ] **Step 2: 어긋난 데가 더 없는지 확인한다**

Run: `grep -n "판정" CLAUDE.md WBS.md PLAN.md`
Expected: 남은 '판정' 문장이 새 규칙과 모순되지 않는다. 모순되면 그 줄도 고친다.

- [ ] **Step 3: 커밋**

```bash
git add CLAUDE.md
git commit -m "docs: 연결 판정을 모델이 하도록 규칙 개정"
```

---

### Task 6: 과거 136일 다시 판정하고 화면 확인

**Files:**
- Modify: `data/articles/*.linked.json` (재생성 — git 에는 안 올라간다)
- Modify: `docs/*.html` (재생성)

**Interfaces:**
- Consumes: Task 1~5 전부
- Produces: 없음

- [ ] **Step 1: 하루치로 먼저 확인한다**

Run: `python link.py 2026-09-02`
Expected: 판정 실패 0건. 흐름이 붙은 기사마다 이유가 보인다.

- [ ] **Step 2: 과거 전체를 다시 판정한다**

`:free` 모델이라 돈은 $0, 다만 30~50분 걸린다. 백그라운드로 돌린다.

```bash
for f in data/articles/[0-9]*-[0-9]*-[0-9]*.json; do
  d=$(basename "$f" .json)
  case "$d" in *.*) continue;; esac
  python link.py "$d"
done
```

Expected: 실패한 날짜가 있으면 그 날짜와 오류 원문이 stderr 에 남는다. **조용히 넘기지 않는다.**

- [ ] **Step 3: 화면을 다시 만든다**

Run: `python render.py --all`
Expected: `뉴스 136일치 → docs/*.html`

- [ ] **Step 4: 완료 조건을 눈으로 확인한다**

Run: `cd docs && python -m http.server 8000`
Expected: `http://localhost:8000/2026-08-20.html` 의 CPO 기사 흐름에서
'슈퍼 모멘텀 출간'·'용인 클린룸'·'DDR4 가격'이 사라지고, 남은 연결마다 이유와 인용구가 보인다.

- [ ] **Step 5: 커밋**

```bash
git add docs
git commit -m "chore: 새 판정 기준으로 흐름 다시 생성"
```

- [ ] **Step 6: 사용자에게 3줄로 보고하고, 배포 여부를 묻는다**

**push 는 사용자가 하라고 할 때만 한다.**
