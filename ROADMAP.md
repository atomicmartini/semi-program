# ROADMAP — 다음에 만들 것

> 작성 2026-08-23. 왜·누구는 `PRD.md`, 규칙은 `CLAUDE.md`, 지금 상태는 `HANDOFF.md`.
> **이 파일은 순서와 이유만 적는다.** 각 기능의 명세는 시작할 때 `docs/slices/` 에 따로 쓴다.

## 하고 싶은 것 5개

1. UI/UX 개선
2. 개념 사전 추가 (매일 + 여러 개 한 번에)
3. 과거와 잇는 **연결고리 기준 높이기**
4. 영어 **번역 + 제대로 된 요약**
5. 그날 내용을 **매일 팟캐스트**로

## 순서와 이유

| 순서 | 무엇 | 왜 이 자리인가 |
| --- | --- | --- |
| **0** | ~~모델 열기~~ ✅ | 4·5 가 여기 걸려 있었다. `google/gemma-4` 무료 풀이 429 로 계속 막혀 **`nvidia/nemotron-3-super-120b-a12b:free`** 로 바꿨다 |
| **1** | **3. 연결고리 기준** | 지금 화면에 **틀린 것처럼 보이는 게 있다** — 주주환원 뉴스와 디자인 어워드가 '삼성전자' 하나로 이어져 있다. `PRD.md` 암묵지대로 관계 하나가 못 믿기면 나머지도 못 믿는다. 모델 불필요, `link.py` 한 파일 |
| **2** | **4. 번역·요약** | 영어 **170건**이 사실상 안 읽힌다. 지금 '요약'은 본문 첫 문장을 자른 것이라 정작 무슨 일인지가 없다. 5 번의 재료이기도 하다 |
| **3** | **2. 개념 사전** | 4 가 끝나면 한국어 요약이 생겨 **어떤 용어가 자주 나오는지**가 정확해진다. 그 뒤에 채워야 헛일이 안 된다 |
| **4** | **1. UI/UX** | 4 가 카드 내용을 바꾸고 2 가 링크를 늘린다. 지금 다듬으면 **두 번 하게 된다** (강의 4장이 디자인을 맨 뒤에 두는 이유) |
| **5** | **5. 팟캐스트** | 4 가 끝나야 대본 재료가 생긴다. 가장 큰 새 갈래 |

### UI 는 두 갈래로 나눈다

| 지금 해도 되는 것 | 4 번 뒤에 할 것 |
| --- | --- |
| 폰에서 깨지는 곳 · 글자 크기 · 날짜 줄이 125개라 길어진 것 | 카드 안 배치 · 흐름 블록 모양 · 배지 |

## 스켈레톤을 어떻게 연장하는가

지금 스켈레톤:

```
수집 → 거르기 → 고르기 → 화면 → 배포
                   └→ 개념 · 흐름
```

**4 번 = 기존 경로에 단계 하나 끼우기 (세로로 두껍게)**

```
고르기 → [번역·요약] → 화면
```

**170건을 한 번에 하지 않는다.** 기사 **1건**을 번역해 배포된 URL 에 뜨는 것까지 먼저 뚫고,
그다음 10건, 그다음 전체.

**5 번 = 새 출력 갈래 만들기 (가로로 새 경로)**

```
그날 기사 → 대본 → 음성 → mp3 → 배포 페이지에서 재생
```

**가짜로라도 끝까지 먼저 뚫는다.** 뉴스 스켈레톤을 뚫었던 것과 같은 방식이다.

1. 하드코딩한 3문장 대본 → TTS 한 번 → mp3 하나 → `<audio>` → 배포. **폰에서 소리가 난다**
2. 그다음 대본을 진짜 기사로
3. 그다음 매일 자동으로

## 미리 정해 둔 위험

| 위험 | 언제 터지나 | 미리 정한 것 |
| --- | --- | --- |
| **팟캐스트 파일이 저장소를 불린다** | 5 번 시작하자마자 | 하루 5MB × 365 = **연 1.8GB**. `docs/` 를 통째로 커밋하는 구조라 그대로 하면 안 된다. **최근 N일만 두고 지우기**를 스켈레톤 뚫을 때 처음부터 넣는다 |
| **NotebookLM 연동이 될지 모른다** | 5 번 시작할 때 | 공개 API 로 오디오를 만들 수 있는지 확실하지 않다. **작게 확인(spike)부터** 하고 안 되면 일반 TTS 로 간다 |
| **무료 모델은 또 막힐 수 있다** | 아무 때나 | 이번에 겪었다. `extract.py` 의 `MODEL` 한 줄만 바꾸면 되게 해 뒀다. 후보는 `nvidia/nemotron-3-nano-30b-a3b:free`(빠름) · `dots-studio/dots-3-note-preview:free` |

## 아직 안 정한 것 — 4 번 시작 전에 답해야 함

**"영어는 해석본으로 URL 에 만들고" 가 두 가지로 읽힌다.**

- **A.** 카드의 요약만 한국어로 바꾼다 (페이지 수 그대로)
- **B.** 기사마다 **번역 전문 페이지를 따로** 만든다

**B 는 `PRD.md` 의 "원문 전체 수집·전문 번역 — 안 함 (저작권)" 과 부딪힌다.**
어느 쪽인지에 따라 작업이 꽤 달라진다.

## 4 번을 시작할 때 첫 슬라이스에 넣을 것

모델을 열면서 알게 된 것 — **`super-120b` 가 영어 기사를 `미분류` 로 틀렸다.**
`filter.py` 는 키워드로 `장비·소재` 를 맞혔는데 `extract.py` 가 그걸 모델 답으로 덮어쓴다.

→ **모델이 `미분류` 를 주면 키워드 분류를 그대로 둔다.**
판정은 코드가 한다는 `CLAUDE.md` 규칙에 맞고, 모델을 바꿔도 안 흔들린다.
