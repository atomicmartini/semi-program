# 프로젝트

반도체 뉴스를 매일 10건 모아 카테고리별로 정리하고, 과거 사건과 잇는다.
사용자는 만든 사람 본인. **5시간 × 3세션짜리 MVP, 프로덕션 아님.**
왜·누구·안 만들 것은 `PRD.md`, 작업 쪼갬은 `WBS.md`, 명세·화면은 `PLAN.md`.

# 실행

실행 명령은 `WBS.md` 작업 명세 표에 둔다. **이 절은 늘리지 않는다.**
**완료 판정은 `WBS.md` 표의 '완료 조건' 명령을 실제로 실행해 확인한다.**
그 명령이 통과하기 전에는 완료라고 말하지 않는다.
배포 https://semi-program.vercel.app — `main` 에 push 하면 자동 반영된다.

# 스택

Python · RSS · 정적 HTML · **Vercel**(배포 — `docs/` 를 발행) · GitHub Actions(매일 실행 — 아직 없음)
서버 없음.
**랭체인·랭그래프 안 씀** — 단계가 고정이라 함수로 충분하다. 임의 변경 금지.
새 라이브러리 추가 전 반드시 질문할 것.

# 규칙

- 사실은 수집한 기사에서만. **기사에 없으면 없는 것이다**
- 관계에는 원문 인용구를 함께 저장한다. **인용구 없는 관계는 쓰지 않는다**
- `[2차]`(언론)만으로 `확정`이라 하지 않는다. `보도됨`으로 둔다
- **점수 매기기·판정·기업명 정규화는 코드가 한다.** 모델은 분류·요약·관계 추출만 한다
- 수집처 주소는 `data/sources.md` 에만. 코드에 박지 않는다
- 기업명은 `data/companies.md` 사전으로만 정규화한다.
  사전에 없으면 추정하지 말고 그 파일 아래 '미확인' 칸에 쌓는다
- **개념 설명은 출처와 확인일 없이 싣지 않는다** (`data/glossary.md`). 모델이 즉석에서 만들지 않는다
- 커밋은 기능 단위 하나씩. 끝나면 **3줄로 보고**한다
- **같은 오류로 두 번 실패하면 고치지 말고 멈춘다.** 시도한 것·오류 원문·다음 후보 2개를 적고 묻는다

# 이 프로젝트의 암묵지

- **기사에 없는 관계를 하나라도 지어내면 나머지 전부를 못 믿는다**
- **피드는 조용히 죽는다.** HTTP 200 인데 기사 0건인 곳이 있었다 (`data/sources.md` '안 쓰는 곳')
- **'뉴스 없음'과 '수집 실패'는 다르다.** 실패는 실패라고 화면에 적는다
- 첫 며칠은 과거분이 없어 '이어지는 흐름'이 비는 게 정상이다. 비었다고 억지로 잇지 않는다

# 하지 말 것

- 원문 전체 저장·게시 — 요약 + 원문 링크만
- 자른 기사 수 숨기기 / 수집 실패 조용히 넘기기
- 카테고리·관계 종류를 임의로 늘리기 — 먼저 질문할 것
- 지어낸 값·빈칸·확인 필요 항목을 보고에서 빼기
- 투자 판단으로 쓰라는 문구·주가·전망 넣기
