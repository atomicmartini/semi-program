# 작업 규칙

반도체 뉴스 데일리 작업 시 반드시 지킬 규칙.
기획은 `PLAN.md`, 사실은 `data/articles/`, 수집처는 `data/sources.md`.

---

## D. 데이터

**D1. 사실은 수집한 기사에서만 가져온다.**
기업·관계·수치는 `data/articles/` 의 기사에서만 읽는다.
사전 지식이나 추론으로 값을 채우지 않는다. 기사에 없으면 없는 것이다.

**D2. 수집처 주소는 `data/sources.md` 에만 적는다.**
코드에 주소를 박지 않는다. 사이트를 더하고 빼는 일은 이 파일만 고쳐서 되어야 한다.

**D3. 기사에 없는 값은 지어내지 않고 빈칸으로 둔다.**
빈칸은 `(빈칸 — 기사에 없음)` 으로 표기하고,
그 항목에 의존하는 판단은 '확인 필요'로 뺀다.

**D4. 기업 이름은 `data/companies.md` 사전으로만 정규화한다.**
사전에 없는 이름은 추정해서 붙이지 않는다.
버리지도 않고 `data/unknown_companies.md` 에 쌓아 사람이 보고 사전에 넣는다.

**D5. 출처는 등급을 표기한다 — `[공식]` / `[2차]`.**
`[공식]`은 기업 IR·보도자료·규제기관 발표를 직접 확인한 것.
`[2차]`는 언론 보도. **`[2차]` 만으로 관계를 '확정'하지 않는다.** '보도됨'으로 둔다.

**D6. 원문 전체를 복제하지 않는다.**
RSS 가 주는 제목·요약과 원문 링크만 저장한다.
본문 전체가 필요하다고 판단되면 기획자에게 먼저 묻는다.

---

## A. 시각과 출처 표기

**A1. 모든 사실 옆에 발행시각과 수집시각을 표기한다.**
기사 목록, 관계 한 줄, 리포트의 각 항목에 모두 노출한다.

**A2. 시각을 모르는 값에는 `—` 를 표기한다.** 추측해서 채우지 않는다.

**A3. 원문 링크를 항상 함께 보여준다.**
링크 없는 주장은 리포트에 싣지 않는다.

---

## R. 판단 규칙

**R1. 관계는 3상태다 — `확정` / `보도됨` / `확인 필요`.**
`[공식]` 출처가 있어야 `확정`. `[2차]` 만 있으면 `보도됨`.
근거가 모자라 단정할 수 없으면 `확인 필요`로 뺀다.
이 프로그램의 목적은 흐름을 정확히 읽는 것이므로, **없는 관계를 지어내는 것이 가장 큰 실패다.**

**R2. 구도는 코드가 도출한다. 모델은 기사에 적힌 관계만 뽑는다.**
"경쟁이다" "협력이다" 를 모델이 판단하게 하지 않는다.
같은 자원을 두고 두 회사가 있으면 경쟁 — 이런 판단은 코드가 한다.

**R3. 관계에는 원문 인용구를 함께 저장한다.**
어느 문장에서 뽑았는지 남기지 않은 관계는 쓰지 않는다.
인용구가 기사 본문에 실제로 있는지 코드로 검산한다.

**R4. 관계 종류는 고정 목록에서만 고른다.**
`공급` `투자` `인수` `제휴` `소송` `증설` `감산` `수주` `규제` `인력이동`
새 종류가 필요하면 임의로 만들지 않고 기획자에게 묻는다.

**R5. 카테고리는 고정 6개 + 미분류다.**
`파운드리·공정` `메모리` `AI·가속기` `장비·소재` `정책·규제` `투자·실적` `미분류`
애매하면 억지로 넣지 않고 `미분류` 로 뺀다. 카테고리는 매일 같아야 비교가 된다.

**R6. 기사 연결은 점수로 판정하고 근거를 남긴다.**
공통 기업·시간 거리·키워드 겹침으로 점수를 낸다.
임계값 아래는 잇지 않고 `확인 필요` 로 둔다. 왜 이었는지 점수와 함께 남긴다.

**R7. 소문과 확정을 화면에서 구분한다.**
`보도됨` 을 `확정` 처럼 보여주지 않는다.
나중에 `[공식]` 이 나와 `확정` 으로 올라가면 그 시점을 기록한다.

**R8. 하루 처리 상한은 10건이다.**
상한을 넘으면 점수 순으로 자르고, **몇 건을 잘랐는지 반드시 밝힌다.**
조용히 버리지 않는다.

**R9. 수집 실패를 조용히 넘기지 않는다.**
사이트가 응답하지 않거나 0건이면 리포트에 그 사실을 적는다.
'오늘 뉴스 없음' 과 '수집 실패' 는 다른 것이다.

**R10. 같은 기사를 두 번 처리하지 않는다.**
원문 링크를 기사의 고유 열쇠로 삼는다.

---

## C. 작업 완료 시

**C1. 작업을 마치면 커밋한다.**

**C2. 무엇을 바꿨는지 3줄로 보고한다.**

**C3. 지어낸 값이 있으면 보고에 반드시 밝힌다.**
빈칸으로 남긴 항목, 확인이 필요한 항목, 잘라낸 기사도 함께 보고한다.
