# HANDOFF — 세션 인수인계

> **다음 세션은 이 파일부터 읽는다.** 그 다음 `CLAUDE.md` → `WBS.md`.
> 새 세션은 지난 대화를 기억하지 못한다. 여기 적힌 것만 안다.
> 규칙은 `CLAUDE.md`, 작업은 `WBS.md`. **여기는 "왜 그렇게 정했나"와 "어디까지 했나"만 적는다.**

## 지금 상태

- 사이트 https://semi-program.vercel.app — **화면은 예시.** 수집·분석 코드는 한 줄도 없다
- 문서 5개(`PRD` `CLAUDE` `WBS` `PLAN` `HANDOFF`) + `data/` 3개 + UI 초안 5개
- 다음에 할 것: **`WBS.md` 의 `3.1 fetch.py`**

---

## 세션 1 — 2026-08-22 (기획·배포)

### 한 일
커밋 19개. `git log --oneline` 으로 볼 것.
기획 문서 → 저장소·배포 → UI 후보 3종 → WBS → 문서 모순 정리.

### 정한 것과 왜

이유가 파일 어디에도 안 남는 것들만 적는다.

| 정한 것 | 왜 |
| --- | --- |
| **랭체인·랭그래프 안 씀** | 수집→거르기→선별→추출→연결→출력이 일직선이고 분기가 없다. 슬라이드 22쪽 판정으로 '그냥 함수' 구간. 다른 프로젝트에서 써봤다는 이유로 끌어오지 말 것 |
| **Vercel 로 배포** (GitHub Pages 아님) | 처음엔 Pages 를 권했으나 Vercel 을 한 번 써보기로 했다. `vercel.json` 의 `outputDirectory: docs` 가 UI 설정을 대신한다. push 후 **약 16초** 뒤 반영되는 것을 실측했다 |
| **공개 저장소** | Vercel·Pages 무료. 수집물이 요약 + 링크라 공개해도 저작권 문제 없다 |
| **UI 는 B 카드 + C 타임라인 합본** | C 단독은 초기에 이어지는 흐름이 0건이라 화면이 텅 빈다. B 는 연결이 없는 날에도 카드가 성립한다 |
| **기업 뉴스룸 3곳 추가** | 언론만 있으면 `[2차]` 뿐이라 **모든 관계가 영원히 `보도됨`** 에 머문다. 소문이 공식 발표로 확정되는 순간을 잡으려면 `[공식]` 채널이 필요하다. 홍보성 잡음은 키워드 거르기에 맡긴다 |
| **개념 탭은 범위 밖** | 설명은 사람이 출처를 달아 넣는 작업이라 코드 짜기와 성격이 다르다. 뉴스가 매일 도는 것을 먼저 확인한다. 화면 초안은 `docs/ui/concepts.html` 에 이미 있다 |
| **`cluster.py` 중복 묶기 범위 밖** | 3세션에 안 들어간다. 없어도 10건은 나온다 |
| **선별 점수에서 `묶음 +1`·`후속 +2` 제거** | 묶음은 중복 묶기가 있어야 생기고, `후속` 은 선별(세션 2) 뒤에 도는 연결(세션 3) 이 만드는 값이라 **순환한다** |
| **카테고리 6 + 미분류 고정** | 매일 같아야 어제와 비교가 된다. 애매하면 억지로 넣지 않고 `미분류` |
| **하루 10건 상한** | 무료 모델 호출 한도. 이 상한 덕에 비용 걱정이 사라졌다 |

### 겪어서 알게 된 것

- **피드는 조용히 죽는다.** Semi Engineering 패키징 카테고리 피드는 HTTP 200 인데 기사가 0건이었다. 응답 코드만 보면 정상으로 보인다
- TSMC·Micron IR 피드는 **403 으로 막혀 있다.** 다른 경로를 찾아야 한다
- 더일렉 반도체 섹션(`S1N2`)은 잡음이 거의 없다. 전체 피드에는 "농심그룹 구글워크스페이스" 같은 게 섞였다
- RSS `description` 에 본문 앞 250자가 들어 있어 **원문을 따로 긁을 필요가 없다**

### 아직 답 못 한 것

| 무엇 | 누가 답할 수 있나 |
| --- | --- |
| 대체재의 **구체적 사건 하나** — 한 번 도는 데 몇 분, 어떤 사이트, "이게 전에 그 얘기랑 이어지나" 싶었던 실제 순간 | 사용자만 |
| 보여줄 **취준생 1명이 누구인가** | 사용자만 |
| Actions 를 매일 몇 시에 돌릴 것인가 | 세션 3 에 정하면 됨 |

### 알면서 안고 가는 위험

- **범위가 여전히 크다.** 남은 작업을 시간으로 재면 두 세션에 안 들어간다.
  `WBS.md` 3절(범위 밖)과 4절(자르는 순서)을 미리 정해 둔 이유다. **막판에 고민하지 말 것**
- 성공 판정이 `7.1 link.py` 하나에 걸려 있다. 밀리면 여기가 아니라 다른 곳을 자른다
- `3.2 backfill` 이 없으면 연결이 빈 화면이 된다. 세션 2 에 반드시 넣는다

### 다음 세션 첫 작업

`WBS.md` 의 **`3.1 fetch.py`**.
완료 조건: `python fetch.py` → `data/articles/<날짜>.json` 생성·건수 출력, 재실행 시 새 기사 0건.
수집처는 `data/sources.md` 의 '쓰는 곳' + '기업 뉴스룸' 8곳. **주소를 코드에 박지 말 것.**

---

## 쓰는 법

세션을 마칠 때 위 형식으로 한 장 덧붙인다. 길게 쓰지 않는다.
**커밋 메시지에 남는 것은 여기 또 적지 않는다** — 두 곳에 적으면 어긋난다.
여기 적을 것은 **커밋에 안 남는 것**, 즉 "왜 그 선택을 했나"와 "무엇을 안 하기로 했나"다.
