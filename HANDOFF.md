# HANDOFF — 세션 인수인계

> **다음 세션은 이 파일부터 읽는다.** 그 다음 `CLAUDE.md` → `WBS.md`.
> 새 세션은 지난 대화를 기억하지 못한다. 여기 적힌 것만 안다.
> 규칙은 `CLAUDE.md`, 작업은 `WBS.md`. **여기는 "왜 그렇게 정했나"와 "어디까지 했나"만 적는다.**

## 지금 상태

**2026-08-29 기준. 세션 8까지 끝났다.**

- 사이트 https://semi-program.vercel.app — 뉴스 · 개념 · **기업** 세 탭
- 데이터 **125일치**(2025-11-03 ~ 2026-08-22) · 거르기 후 207건
- 코드 `fetch.py` `archive.py` `filter.py` `pick.py` `extract.py` `link.py`
  `glossary.py` `companies.py` `render.py` — **의존성 0개**
- 테스트 **73개** (`python -m unittest discover -p "test_*.py"`)
- 개념 5개 · 기업 38곳(8분류) · **회사 설명 38곳 전부 채워짐**
- 커밋·push 다 끝났고 작업트리가 깨끗하다

**한 바퀴 돌리는 법**
```
python fetch.py                  # RSS 8곳에서 받기
python extract.py <날짜>          # 모델로 한국어 요약 (하루 50건 한도)
python link.py <날짜>             # 이어지는 흐름
python render.py --all           # docs/ 에 HTML 생성
git add docs && git commit && git push    # 15초 뒤 사이트 반영
```

**바로 다음에 할 것 — `ROADMAP.md` 순서: 개념 사전 → UI/UX → 팟캐스트.**

---

## 세션 1 — 2026-08-22 (기획·배포)

### 한 일
커밋 19개. `git log --oneline` 으로 볼 것.
기획 문서 → 저장소·배포 → UI 후보 3종 → WBS → 문서 모순 정리.

### 정한 것과 왜

이유가 파일 어디에도 안 남는 것들만 적는다.

| 정한 것 | 왜 |
| --- | --- |
| **랭체인·랭그래프 안 씀** | 수집→거르기→선별→추출→연결→출력이 일직선이고 분기가 없다. 슬라이드 22쪽 판정으로 '그냥 함수' 구간. 다른 프로젝트에서 써봤다는 이유로 끌어오지 말 것 |
| **Vercel 로 배포** (GitHub Pages 아님) | 처음엔 Pages 를 권했으나 Vercel 을 한 번 써보기로 했다. `vercel.json` 의 `outputDirectory: docs` 가 UI 설정을 대신한다. push 후 **약 16초** 뒤 반영되는 것을 실측했다 |
| **공개 저장소** | Vercel·Pages 무료. 수집물이 요약 + 링크라 공개해도 저작권 문제 없다 |
| **UI 는 B 카드 + C 타임라인 합본** | C 단독은 초기에 이어지는 흐름이 0건이라 화면이 텅 빈다. B 는 연결이 없는 날에도 카드가 성립한다 |
| **기업 뉴스룸 3곳 추가** | 언론만 있으면 `[2차]` 뿐이라 **모든 관계가 영원히 `보도됨`** 에 머문다. 소문이 공식 발표로 확정되는 순간을 잡으려면 `[공식]` 채널이 필요하다. 홍보성 잡음은 키워드 거르기에 맡긴다 |
| **개념 탭은 범위 밖** | 설명은 사람이 출처를 달아 넣는 작업이라 코드 짜기와 성격이 다르다. 뉴스가 매일 도는 것을 먼저 확인한다. 화면 초안은 `docs/ui/concepts.html` 에 이미 있다 |
| **`cluster.py` 중복 묶기 범위 밖** | 3세션에 안 들어간다. 없어도 10건은 나온다 |
| **선별 점수에서 `묶음 +1`·`후속 +2` 제거** | 묶음은 중복 묶기가 있어야 생기고, `후속` 은 선별(세션 2) 뒤에 도는 연결(세션 3) 이 만드는 값이라 **순환한다** |
| **카테고리 7 + 미분류 고정** | 매일 같아야 어제와 비교가 된다. 애매하면 억지로 넣지 않고 `미분류`. 패키징은 67일치에서 48% 를 차지해 `파운드리·공정` 에서 분리했다 |
| **하루 10건 상한** | 무료 모델 호출 한도. 이 상한 덕에 비용 걱정이 사라졌다 |

### 겪어서 알게 된 것

- **피드는 조용히 죽는다.** Semi Engineering 패키징 카테고리 피드는 HTTP 200 인데 기사가 0건이었다. 응답 코드만 보면 정상으로 보인다
- TSMC·Micron IR 피드는 **403 으로 막혀 있다.** 다른 경로를 찾아야 한다
- 더일렉 반도체 섹션(`S1N2`)은 잡음이 거의 없다. 전체 피드에는 "농심그룹 구글워크스페이스" 같은 게 섞였다
- RSS `description` 에 본문 앞 250자가 들어 있어 **원문을 따로 긁을 필요가 없다**

### 아직 답 못 한 것

| 무엇 | 누가 답할 수 있나 |
| --- | --- |
| 대체재의 **구체적 사건 하나** — 한 번 도는 데 몇 분, 어떤 사이트, "이게 전에 그 얘기랑 이어지나" 싶었던 실제 순간 | 사용자만 |
| 보여줄 **취준생 1명이 누구인가** | 사용자만 |
| Actions 를 매일 몇 시에 돌릴 것인가 | 세션 3 에 정하면 됨 |

### 알면서 안고 가는 위험

- **범위가 여전히 크다.** 남은 작업을 시간으로 재면 두 세션에 안 들어간다.
  `WBS.md` 3절(범위 밖)과 4절(자르는 순서)을 미리 정해 둔 이유다. **막판에 고민하지 말 것**
- 성공 판정이 `7.1 link.py` 하나에 걸려 있다. 밀리면 여기가 아니라 다른 곳을 자른다
- `3.2 backfill` 이 없으면 연결이 빈 화면이 된다. 세션 2 에 반드시 넣는다

---

## 세션 2 — 2026-08-22 (수집·거르기)

### 한 일
`fetch.py` (WBS 3.1·3.2) · `filter.py` + `data/keywords.md` (WBS 4.1).

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **의존성 0개** | `feedparser` 없이 `urllib` + `xml.etree` 로 8곳 전부 파싱된다. Actions 설정이 단순해지고 가상환경도 필요 없다 |
| **발행일 기준으로 저장** | `data/articles/<발행일>.json`. 이렇게 하니 **backfill(3.2)이 공짜로 끝났다** — 첫 실행에 67일치가 쌓였다 |
| **기업명을 `companies.md` 에서 읽어 포함 판정에 씀** | `"SK하이닉스 성과급 합의"` 처럼 반도체 단어가 없는 기사가 버려지고 있었다. 기업 목록을 `keywords.md` 에 또 적으면 어긋난다 |
| **같은 제목 중복 제거를 `filter.py` 에 넣음** | 비슷한 기사 묶기(`cluster.py`)는 범위 밖이지만, 제목이 **완전히 같은 것**만 거르는 건 3줄이다 |
| **패키징을 별도 카테고리로 분리** | 처음엔 `파운드리·공정` 에 넣었는데 67일치에서 **48%** 를 차지했다. 분리 후 25.9% / 22.4% 로 갈렸다 |

### 겪어서 알게 된 것

- **SK하이닉스 뉴스룸은 한 기사를 여러 페이지로 쪼개 올린다** (`...-1/` ~ `...-6/`).
  링크가 달라서 URL 중복 제거를 통과한다. 제목으로 한 번 더 걸러야 한다
- **`curl` 로 200 이어도 파이썬에서 403 이 날 수 있다.** 3D InCites·Intel Newsroom 이 그랬다.
  `User-Agent`·`Accept`·`Accept-Language` 를 채우면 풀린다. **"확인했다"가 접근 방법에 따라 다르다**
- 워드프레스 피드는 요약에 HTML 과 `The post ... appeared first on ...` 꼬리표를 담아 보낸다
- 날짜 형식이 두 가지다 — 더일렉만 `2026-08-21 18:08:15`, 나머지 7곳은 RFC 822
- 기업 뉴스룸에는 채용·가전 기사가 섞여 온다. 제외 키워드가 계속 필요하다

### 남은 것

`4.2 pick.py` → `5.1 render.py` → (세션 3) `6.1 extract.py` → `7.1 link.py` → `8.1 daily.yml`

---

## 세션 3 — 2026-08-23 (스켈레톤 + 화면)

### 한 일
`PLAN.md` 다시 씀 → 개념 사전 출처 확보 → `pick.py` 검증 → `render.py` → 개념 페이지 → 칩 필터.
커밋 8개. **오늘 계획한 기능 3개가 전부 배포됐다.**

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **오늘은 모델을 한 번도 안 쓴다** | 오늘의 목표는 '경로를 끝까지 뚫는 것'. 모델을 넣으면 열쇠·비용·환각이 한꺼번에 들어와 화면을 못 띄운 채 끝날 수 있다 |
| **10건은 상한, 있는 만큼만** | 67일 중 10건 이상인 날이 **2일뿐**이다. 억지로 채우면 지어내게 된다 |
| **`select.py` → `pick.py` 로 이름 변경** | `select` 는 파이썬 표준 라이브러리 이름이다. 아래 '겪어서 알게 된 것' 참조 |
| **용어는 등장 횟수를 세어 위에서부터** | 무작위로 5개를 고르지 않았다. 143건에서 실제로 몇 번 나오는지 세었다. 안 나오는 용어를 넣으면 링크가 안 걸린다 |
| **`NAND`·`HBM` 은 넣지 않았다** | 삼성 용어사전에 없어 출처를 못 붙였다. 첫 화면을 보기 좋게 하려고 출처 없이 넣으면 **오늘 만든 규칙을 오늘 어기는 것** |
| **0건 카테고리도 칩에 보여준다** | 오늘 무엇이 비었는지가 정보다. 흐리게 하고 못 누르게만 했다 |
| **`PLAN.md` → `PLAN_pre.md` 로 보관** | 새 판을 쓰면서 옛 판을 지우지 않았다. 다만 **살아 있는 명세는 새 `PLAN.md` 로 옮겼다** — 코드가 '보관용' 파일을 참조하면 안 된다 |

### 겪어서 알게 된 것

- **`select.py` 라는 파일 이름은 파이썬을 망가뜨린다.** `import select` 가 표준 라이브러리 대신 우리 파일을 불러온다.
  윈도우에서는 티가 안 나지만 **리눅스(GitHub Actions)에서는 `subprocess`·`asyncio` 가 표준 `select` 를 써서 깨진다.**
  `string.py` `json.py` `time.py` 같은 이름도 마찬가지다
- **삼성반도체 용어사전은 URL 패턴이 규칙적이다** — `.../semiconductor-glossary-<영문>/`.
  다만 `NAND` `HBM` `memory` 는 **없다.** 그쪽은 JEDEC 등 다른 출처를 찾아야 한다
- **어떤 날은 사전 용어가 기사에 하나도 안 나온다.** 08-22 는 0개, 08-20 은 3개였다.
  기능이 멀쩡해도 **첫 화면에서는 안 보일 수 있다** — 그래서 날짜 링크가 필요했다
- Vercel 반영은 push 후 **5~16초**

### 남은 것

`extract.py`(모델) → `link.py`(이어지는 흐름) → `daily.yml`(자동 실행).
`cluster.py` · 기업명 정규화 · 용어 자동 수집 · 슬라이스 폴더 나누기는 그 뒤.

---

## 세션 4 — 2026-08-23 (extract.py, 막힘)

### 한 일
`extract.py` 작성 — 모델 호출(`urllib`, 의존성 추가 없음) → 코드가 카테고리를 정해진
목록으로 강제 → **관계의 인용구가 원문에 실제로 있는지 대조해 없으면 그 관계를 버림.**
`.env` 를 `my-app` 에서 복사해 열쇠를 마련함 (`.gitignore` 에 있어 커밋 안 됨).

### 막힌 것

**`google/gemma-4-26b-a4b-it:free` 가 OpenRouter 상위 제공자(Google AI Studio)
공유 무료 풀에서 429 로 막혀 있다.** 기사 1건 호출 → 즉시 429 → 20초 뒤 재시도 →
같은 429 → **20초 간격 5회(총 100초)** 더 재시도 → 전부 429.
`CLAUDE.md` 규칙대로 같은 오류 두 번째에 멈추고 사용자에게 물었고,
**"오늘은 여기서 멈춤"** 으로 답을 받았다. 코드 문제가 아니라 그 모델 자체가
지금 혼잡한 것으로 보인다 (`limit_source: upstream_provider_shared_pool`).

### 다음 세션이 먼저 할 것

1. 기사 **1건**으로 `python -c` 호출을 다시 해 보고 (아래 명령), 여전히 429 면
2. **다른 무료 모델로 교체할지 사용자에게 물을 것** — 후보를 조사해 제시한다
   (`extract.py` 의 `MODEL` 상수 한 줄만 바꾸면 된다)

```
python -c "
from pick import select_day
from filter import read_keywords, UNCLASSIFIED
from extract import extract_one, _load_key
picked, _, _ = select_day('2026-08-22')
_, _, cats = read_keywords()
a = picked[0]
print(extract_one(a, [*cats, UNCLASSIFIED], _load_key()))
"
```

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **`urllib` 로 OpenRouter 를 직접 호출, 랭체인 안 씀** | `CLAUDE.md` 19행(랭체인 안 씀)과 의존성 0개를 지킴. `fetch.py` 가 이미 하는 방식과 같다 |
| **관계는 타입을 나누지 않는다** (경쟁/협력 등 열거하지 않음) | 관계 종류를 여러 개 만들려면 먼저 질문해야 한다 (`CLAUDE.md`). 지금은 `{companies, description, quote}` 하나뿐 |
| **인용구가 원문(제목+요약)에 없으면 그 관계를 조용히 버림** | "인용구 없는 관계는 쓰지 않는다" 를 실행 가능한 코드 검사로 바꾼 것. 모델이 그럴듯한 인용구를 지어내도 걸린다 |
| **카테고리는 모델이 고르되, 목록에 없으면 코드가 `미분류` 로 강제** | 키워드 분류(19% 미분류)보다 모델이 더 잘 나눌 걸로 기대했으나 **아직 검증 못 함** |

---

## 세션 5 — 2026-08-23 (슬라이싱 도입 · 이어지는 흐름)

### 한 일

**강의 PDF 2차(3장 '슬라이스') 방식을 여기서부터 적용하기 시작했다.**
`docs/slices/` 에 기능마다 스펙 한 장을 먼저 쓰고 → 실패하는 테스트 → 구현 → 배포.

| 슬라이스 | 결과 |
| --- | --- |
| `01-이어지는-흐름` | `link.py` · `test_link.py` (5개 통과). 08-22 기사 4건 중 2건이 과거와 연결됨 |
| `02-이어지는-흐름-화면` | `render.py` 에 타임라인 붙임 · `test_render.py` (4개 통과). 배포 확인 완료 |

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **이미 배포된 기능 3개는 `docs/slices/` 로 소급해 쓰지 않는다** | 코드가 이미 통과하는 상태에서 "실패하는 테스트"를 사후에 지어내면 3장의 목적(스펙이 설계 오류를 미리 잡음)이 사라진다. 그 셋의 '왜'는 이 파일과 `WBS.md` 가 이미 담고 있다 |
| **`extract.py` 를 기다리지 않고 `link.py` 를 먼저 했다** | `WBS.md` 4절이 이미 준비해 둔 완화책("연결은 기업명 겹침만으로 판정")을 그대로 썼다. 막힌 것 하나가 전체를 세우지 않게 하는 장치가 실제로 작동했다 |
| **공통 기업 1개 · 과거 범위 제한 없음 · 화면은 다음 슬라이스** | 지금 데이터(143건)로 2개 이상을 요구하면 거의 안 잡힌다. 화면을 같이 넣으면 슬라이스가 40분을 넘는다 |
| **배지(`확정`/`보도됨`)와 '달라진 점' 문구는 안 넣었다** | 근거가 공통 기업 언급뿐이라 배지를 달면 **있는 근거보다 과한 주장**이 된다. `extract.py` 의 인용구 검증이 살아난 뒤에 붙인다 |
| **테스트는 `unittest`** | `pytest` 가 없고, 의존성 0개를 유지하려면 표준 라이브러리여야 한다 |

### 겪어서 알게 된 것

- **`filter.py`/`pick.py` 가 만드는 파생 파일(`.filtered/.selected/.linked`)이 날짜 glob 에 섞인다.**
  `"." in path.stem` 으로 걸러야 한다. `render.py` 는 이미 하고 있었고 `link.py` 에도 같은 장치를 넣었다
- **공통 기업 1개 기준은 헐겁다.** 주주환원 뉴스와 디자인 어워드 뉴스가 '삼성전자' 하나로 묶였다.
  설계상 예상된 결과지만, 화면에서 보니 눈에 띈다. 임계값을 올리거나 기업 가중치를 두는 것을 나중에 검토
- `link.py` 는 최신순으로 주는데 **화면 타임라인은 오래된 것부터**여야 한다. 정렬을 화면 쪽에서 뒤집는다

### 남은 것

`extract.py`(막힘) · `daily.yml`(8.1)

---

## 세션 6 — 2026-08-23 (과거 기사 수집)

### 한 일

슬라이스 `03-과거-기사-수집` — `archive.py` · `test_archive.py` (12개 통과).
**데이터가 67일 → 125일로 늘었다** (`2025-11-03 ~ 2026-08-22`).

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **RSS 가 아니라 목록 페이지를 읽는다** | RSS 는 최근분만 준다. 과거는 목록 페이지밖에 길이 없다. `fetch.py` 는 안 건드리고 `archive.py` 를 따로 뒀다 |
| **기사별 원문 페이지는 안 연다** | 두 사이트 다 **목록에 제목·요약·날짜가 전부** 있었다. 원문까지 열면 요청이 몇 배가 된다 |
| **기간을 `2025-11~2026-04` 로 밀었다** | 사용자 답은 "최근 6개월"이었으나 그러면 05·06·07 이 **이미 있는 달**이라 절반이 중복이 된다. 분량은 그대로 두고 창만 뒤로 밀었다 |
| **삼성 뉴스룸은 뺐다** | `/kr/latest/page/N`(N≥2) 이 4회 모두 연결 실패. 유일한 `[공식]` 후보라 아쉽지만 **확인 안 된 것을 넣지 않는다** |
| **달마다 고르게 벌려서 5건** | 5건이 같은 날에 몰리면 '이어지는 흐름' 재료로 쓸모가 없다 |

### 겪어서 알게 된 것

- **더일렉은 쪽마다 날짜 형식이 다르다.** 과거 쪽은 `2025-02-06`, **최근 쪽은 `08-21 18:08` 로 연도가 없다.**
  이걸 못 읽으니 시작 쪽을 찾는 이분 탐색이 1쪽으로 무너져 **"0건 파싱됨"** 이 났다.
  연도 없는 날짜는 올해로 보되 미래가 되면 작년으로 돌린다
- **`MAX_PAGES` 를 12 로 뒀더니 6개월을 못 덮었다.** 더일렉은 쪽당 6~9일치라 6개월에 25쪽 이상 필요하다. 30으로 올렸다
- **같은 명령을 두 번 돌리면 새 기사 0건이지만, 기간을 바꾸면 몇 건 더 들어온다.**
  고르는 지점이 모은 기사 수에 따라 정해지기 때문이다. 링크 중복은 안 생기니 해롭지는 않다

### 다음 세션이 먼저 볼 것 — **과거분이 화면에 안 보인다**

**데이터는 늘었는데 화면은 그대로다.** 08-22 기사 하나에 공통 기업이 있는 과거 기사가
**20건(2025-11~2026-08)** 이나 되는데, `find_related` 가 **최신순 상위 3건**만 잡아
셋 다 2026-08 에서 나온다. 2025 년 것은 한 번도 안 보인다.

`PRD.md` 가 노리는 "이게 전에 그 얘기랑 이어지나" 는 **오래된 연결**에서 나온다.
→ 다음 슬라이스 후보: `find_related` 의 고르는 규칙을 바꾼다
(시간에 걸쳐 벌리기 / 공통 기업 수를 먼저 보기 / 가장 오래된 것 한 건은 반드시 넣기).
**슬라이스 03 범위 밖이라 이번엔 코드를 안 고쳤다.**

---

## 세션 7 — 2026-08-23~24 (화면·요약·연결·기업 탭)

슬라이스를 네 개 했다. 스펙은 `docs/slices/` 에 있다.

| 슬라이스 | 결과 |
| --- | --- |
| `04-과거가-보이게` | `render.py` 가 최근 14일만 만들어 과거분이 화면에 아예 없었다. **125일 전부** 만들고 날짜 줄을 달별로 묶었다. 흐름 상한 3 → 10 |
| `05-한국어-요약` | `extract.py` 가 만든 `summary_ko` 를 화면이 안 읽고 있었다. 카드가 그걸 쓰게 하고 `AI 요약` 표시를 붙였다. 제목은 영어 그대로 |
| `06-연결고리-기준` | **(기업 직접 겹침 OR 관계로 이어진 기업) AND 키워드 겹침** 으로 바꿨다. 주주환원 기사의 흐름이 3건 → 1건 |
| `07-기업-탭` | 새 탭. 밸류체인 8분류 · 회사 38곳 · 회사별 기사 페이지 |

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **모델을 `nvidia/nemotron-3-super-120b-a12b:free` 로 바꿨다** | `google/gemma-4` 무료 풀이 계속 429 였다. `extract.py` 의 `MODEL` 한 줄만 바꾸면 갈아탈 수 있다 |
| **모델이 `미분류` 를 주면 키워드 분류를 살린다** | `super-120b` 가 영어 기사를 미분류로 틀렸는데 `filter.py` 는 맞혔다. **판정은 코드가 한다** |
| **기업 분류는 뉴스 카테고리와 다른 체계** | 뉴스는 '무엇에 관한 기사인가', 기업은 '밸류체인 어디인가'. 팹리스·OSAT 는 뉴스 카테고리에 없다. 의도한 것이다 |
| **한 회사가 여러 분류에 나온다** | 삼성전자는 메모리이자 파운드리다. 대표 하나로 박으면 파운드리 칸에서 삼성이 빠져 사실과 달라진다 |
| **기업은 출처가 없어도 목록에서 빼지 않는다** | 개념 사전과 다른 점. 여기서는 **목록 자체가 값**이라 회사를 감추면 분류표가 망가진다. 설명만 비운다 |
| **`companies.md` 표를 넓혔다. 새 파일을 안 만들었다** | 두 곳에 적으면 어긋난다. 열을 **뒤에** 붙이면 `filter.py`·`link.py` 가 안 깨진다(확인함) |

### 겪어서 알게 된 것

- **`intel` 이 `intelligent`·`intelligence` 안에서 잡혀 인텔 기사 23건 중 7건이 거짓이었다.**
  `companies_mentioned` 가 그냥 부분 문자열 포함이었다. `ASE`(→`release`)·`AMD` 같은 짧은 이름을
  넣기 전에 걸렸다. **영문 별칭은 낱말 경계를 요구**하도록 고쳤다 (한국어는 조사가 붙어 그대로 둔다).
  → 새 별칭을 넣을 때는 **짧은 영문 이름을 조심**할 것. `arm`·`ti` 는 흔한 낱말이라 아예 안 넣었다
- **OpenRouter 무료 등급은 하루 50건이다.** `X-RateLimit-Limit: 50`. 재시도로 안 풀린다.
  `extract.py` 는 이미 뽑은 기사를 건너뛰므로 다음 날 이어서 돌리면 된다
- **HTTP 200 인데 `choices` 가 없는 응답이 온다.** 그때 터지면 앞서 처리한 기사까지 날아간다
- **회사 공식 사이트 상당수가 봇을 막는다** — SK하이닉스·Applied Materials·인텔·AMD·KLA·
  케이던스·ASE·Amkor 가 403/404. **검색으로 정확한 주소를 찾아도 막혔고, sec.gov 도 403 이었다.**
  그래서 38곳 중 **설명은 12곳만** 채웠다

### 남은 것 — 다음 세션이 할 일

**회사 설명 26곳.** `claude --chrome` 으로 켜면 사용자 브라우저를 통해 읽을 수 있어
막힌 곳이 열린다. 우선순위는 기사가 많은 곳 — 인텔(16건) · SK하이닉스(15건) ·
AMD(6건) · 어플라이드머티어리얼즈(4건) · 한미반도체(3건).
`data/companies.md` 표의 `설명`·`출처`·`확인일` 세 칸만 채우고 `python render.py --all`.

그 뒤는 `ROADMAP.md` 순서 — 개념 사전 → UI/UX → 팟캐스트.

---

## 세션 8 — 2026-08-29 (회사 설명 26곳)

### 한 일

세션 7 이 못 채운 **회사 설명 26곳을 전부 채웠다.** 표의 `설명`·`출처`·`확인일` 세 칸만 건드렸다.
코드는 한 줄도 안 고쳤다. 테스트 73개 그대로 통과.

### 정한 것과 왜

| 정한 것 | 왜 |
| --- | --- |
| **`claude --chrome` 으로 사용자 브라우저를 통해 읽었다** | 세션 7 이 403 으로 막혔던 SK하이닉스·인텔·AMD·Applied Materials·KLA·케이던스·ASE·앰코가 **전부 열렸다.** 봇 차단은 접근 경로의 문제였지 주소의 문제가 아니었다 |
| **회사가 스스로 1위라고 한 것은 '밝힌다' 로 적었다** | 한미반도체(TC 본더 세계 1위)·미디어텍(세계 5위 팹리스)·SK실트론(국내 유일). 검증한 사실이 아니라 **회사의 자기 주장**이라 그렇게 읽히도록 썼다 |
| **매출·직원 수 같은 숫자는 안 넣었다** | 해마다 바뀌는데 `확인일` 하나로는 낡은 것을 알아챌 수 없다. 기존 12곳도 숫자를 안 쓰고 있었다 |
| **읽은 페이지 하나만 출처로 달았다** | 여러 쪽을 합쳐 쓰면 어느 문장이 어디서 왔는지 못 되짚는다. SK하이닉스는 브랜드 페이지가 구호뿐이라 **제품 포트폴리오 쪽**을 출처로 삼았다 |

### 겪어서 알게 된 것

- **마크다운 표 칸에 `|` 가 들어가면 열이 하나 늘어 행이 조용히 깨진다.**
  케이던스 출처를 `[About Us | Cadence]` 로 적었더니 열이 9개가 되어 `확인일` 이 화면에서 사라졌다.
  38쪽을 전부 훑어 `확인일` 없는 쪽을 찾고서야 알았다. **표에 넣는 링크 제목에서 `|` 를 뺀다**
- **`about` 주소는 대부분 죽어 있다.** AMD·램리서치·앰코·ASE·JCET·UMC·ST·신에츠 8곳이 404 였다.
  주소를 짐작하지 말고 **홈에서 링크를 긁어** 찾는 편이 빨랐다
- **회사 소개가 글이 아니라 그림인 곳이 있다** — Applied Materials·DB하이텍·JCET·하나마이크론은
  `get_page_text` 가 빈손이라 화면을 찍어 읽었다
- **`www` 가 붙고 안 붙고에 따라 열리고 안 열린다.** `www.hanamicron.com` 은 연결 실패,
  `hanamicron.com` 은 정상이었다. 두 번 실패했다고 그 사이트를 포기할 뻔했다

### 남은 것

`ROADMAP.md` 순서 — 개념 사전 → UI/UX → 팟캐스트.

---

## 쓰는 법

세션을 마칠 때 위 형식으로 한 장 덧붙인다. 길게 쓰지 않는다.
**커밋 메시지에 남는 것은 여기 또 적지 않는다** — 두 곳에 적으면 어긋난다.
여기 적을 것은 **커밋에 안 남는 것**, 즉 "왜 그 선택을 했나"와 "무엇을 안 하기로 했나"다.
